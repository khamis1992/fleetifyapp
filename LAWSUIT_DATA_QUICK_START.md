# 🚀 دليل سريع - تحديث صفحة بيانات التقاضي

## ✅ ما تم إضافته؟

تم تحديث صفحة **بيانات التقاضي** (`/legal/lawsuit-data`) لتشمل **9 أعمدة جديدة** من التقارير:

### 📊 الأعمدة الجديدة

| المصدر | الأعمدة |
|--------|---------|
| 🔵 **المذكرة الشارحة** | عدد الأشهر المتأخرة • قيمة الإيجار المتأخر • غرامات التأخير • عدد الأيام المتأخرة |
| 🟡 **كشف المطالبات المالية** | عدد الفواتير المتأخرة • إجمالي المبالغ المستحقة • إجمالي الغرامات |
| 🔴 **كشف المخالفات المرورية** | عدد المخالفات • قيمة المخالفات |

---

## 🔧 خطوات التفعيل

### 1️⃣ تنفيذ SQL Migration

```bash
# في مجلد المشروع
cd supabase

# تطبيق الـ migration
supabase db push

# أو يدوياً عبر Supabase Dashboard
# انسخ محتوى: supabase/migrations/20260131000000_add_lawsuit_report_columns.sql
# والصقه في SQL Editor
```

### 2️⃣ التحقق من الأعمدة

```sql
-- تحقق من إضافة الأعمدة
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'lawsuit_templates'
AND column_name IN (
  'months_unpaid', 'overdue_amount', 'late_penalty', 'days_overdue',
  'invoices_count', 'total_invoices_amount', 'total_penalties',
  'violations_count', 'violations_amount'
);
```

### 3️⃣ اختبار الصفحة

```bash
# شغل dev server
npm run dev

# افتح الصفحة
http://localhost:8080/legal/lawsuit-data
```

---

## 📱 كيفية الاستخدام

### عرض البيانات
1. افتح صفحة **بيانات التقاضي**
2. شاهد الأعمدة الجديدة بألوان مميزة:
   - 🔵 **أزرق** = المذكرة الشارحة
   - 🟡 **كهرماني** = كشف المطالبات
   - 🔴 **أحمر** = كشف المخالفات

### البطاقات الإحصائية
- **إجمالي الفواتير المتأخرة** - عدد كل الفواتير
- **إجمالي المخالفات** - عدد كل المخالفات
- **إجمالي الإيجار المتأخر** - مجموع الإيجارات
- **إجمالي الغرامات** - مجموع كل الغرامات
- **إجمالي قيمة المخالفات** - مجموع قيم المخالفات

### تصدير Excel
1. اضغط على زر **"تصدير Excel"**
2. سيتم تحميل ملف يحتوي على **جميع الأعمدة** (25 عمود)
3. افتح الملف في Excel لمراجعة البيانات

---

## 🔄 ملء البيانات للقضايا الجديدة

### تلقائياً من صفحة التحضير

عند إنشاء قضية جديدة من صفحة **تحضير القضية**:

```
/legal/lawsuit/prepare/{contract_id}
```

سيتم حساب وحفظ جميع البيانات تلقائياً:

```typescript
// مثال: البيانات المحسوبة تلقائياً
{
  months_unpaid: 3,              // من عدد الفواتير المتأخرة
  overdue_amount: 9000,          // من مجموع الإيجارات
  late_penalty: 1200,            // من حساب الغرامات
  days_overdue: 90,              // من تاريخ البداية
  invoices_count: 3,             // عدد الفواتير
  total_invoices_amount: 9000,   // مجموع الفواتير
  total_penalties: 1200,         // مجموع الغرامات
  violations_count: 5,           // عدد المخالفات
  violations_amount: 2500        // مجموع المخالفات
}
```

---

## 🛠️ تحديث القضايا القديمة

### الطريقة 1: يدوياً (للقضايا القليلة)

```sql
-- تحديث قضية واحدة
UPDATE lawsuit_templates
SET 
  months_unpaid = 3,
  overdue_amount = 9000,
  late_penalty = 1200,
  days_overdue = 90,
  invoices_count = 3,
  total_invoices_amount = 9000,
  total_penalties = 1200,
  violations_count = 5,
  violations_amount = 2500
WHERE id = 123;
```

### الطريقة 2: باستخدام الدالة

```sql
-- استخدام الدالة المساعدة
SELECT update_lawsuit_report_data(
  p_lawsuit_id := 123,
  p_months_unpaid := 3,
  p_overdue_amount := 9000,
  p_late_penalty := 1200,
  p_days_overdue := 90,
  p_invoices_count := 3,
  p_total_invoices_amount := 9000,
  p_total_penalties := 1200,
  p_violations_count := 5,
  p_violations_amount := 2500
);
```

### الطريقة 3: Script تلقائي (قريباً)

سيتم إضافة script لتحديث جميع القضايا القديمة تلقائياً من بيانات العقود والفواتير.

---

## 📊 استعلامات مفيدة

### القضايا ذات الإيجار المتأخر الأعلى

```sql
SELECT 
  case_title,
  defendant_first_name || ' ' || defendant_last_name AS defendant_name,
  overdue_amount,
  months_unpaid,
  late_penalty
FROM lawsuit_templates
WHERE overdue_amount > 0
ORDER BY overdue_amount DESC
LIMIT 10;
```

### القضايا ذات المخالفات الأكثر

```sql
SELECT 
  case_title,
  defendant_first_name || ' ' || defendant_last_name AS defendant_name,
  violations_count,
  violations_amount
FROM lawsuit_templates
WHERE violations_count > 0
ORDER BY violations_count DESC
LIMIT 10;
```

### إحصائيات شاملة

```sql
SELECT 
  COUNT(*) AS total_cases,
  SUM(months_unpaid) AS total_months_unpaid,
  SUM(overdue_amount) AS total_overdue,
  SUM(late_penalty) AS total_late_penalties,
  SUM(invoices_count) AS total_invoices,
  SUM(total_invoices_amount) AS total_invoices_amount,
  SUM(violations_count) AS total_violations,
  SUM(violations_amount) AS total_violations_amount,
  -- الإجمالي الكلي
  SUM(
    COALESCE(overdue_amount, 0) + 
    COALESCE(late_penalty, 0) + 
    COALESCE(total_penalties, 0) + 
    COALESCE(violations_amount, 0)
  ) AS grand_total
FROM lawsuit_templates;
```

### استخدام الـ View الموحد

```sql
-- عرض القضايا مع الحسابات التلقائية
SELECT 
  case_title,
  grand_total,                -- الإجمالي الكلي
  avg_monthly_overdue,        -- متوسط الإيجار الشهري
  avg_violation_amount,       -- متوسط قيمة المخالفة
  avg_invoice_amount          -- متوسط قيمة الفاتورة
FROM lawsuit_templates_with_totals
ORDER BY grand_total DESC
LIMIT 20;
```

---

## 🎨 التخصيص

### تغيير الألوان

في `LawsuitDataPage.tsx`:

```typescript
// المذكرة الشارحة
className="bg-blue-50"    // غير إلى اللون المطلوب

// كشف المطالبات
className="bg-amber-50"   // غير إلى اللون المطلوب

// كشف المخالفات
className="bg-red-50"     // غير إلى اللون المطلوب
```

### إضافة فلاتر

```typescript
// مثال: فلتر حسب عدد الأشهر المتأخرة
const filteredByMonths = lawsuits.filter(
  l => l.months_unpaid >= minMonths && l.months_unpaid <= maxMonths
);
```

---

## ❓ الأسئلة الشائعة

### س: هل يمكن إخفاء بعض الأعمدة؟
**ج:** نعم، يمكنك التعليق على الأعمدة غير المرغوبة في الكود.

### س: هل البيانات القديمة ستُحدث تلقائياً؟
**ج:** لا، القضايا القديمة تحتاج تحديث يدوي أو عبر script.

### س: كيف أضيف أعمدة جديدة؟
**ج:** 
1. أضف العمود في SQL
2. أضف في TypeScript interface
3. أضف في الجدول والتصدير

### س: هل يمكن تصدير بيانات معينة فقط؟
**ج:** نعم، عدّل دالة `handleExportToExcel` لتصفية البيانات.

---

## 🐛 حل المشاكل

### الأعمدة لا تظهر
```bash
# تحقق من تطبيق الـ migration
supabase db diff

# أعد تشغيل dev server
npm run dev
```

### البيانات تظهر 0
```bash
# تحقق من البيانات في قاعدة البيانات
SELECT * FROM lawsuit_templates LIMIT 1;

# إذا كانت NULL، حدثها إلى 0
UPDATE lawsuit_templates 
SET months_unpaid = COALESCE(months_unpaid, 0);
```

### خطأ في التصدير
```bash
# تحقق من مكتبة XLSX
npm install xlsx

# أعد تشغيل
npm run dev
```

---

## 📚 الملفات المرجعية

- **الكود**: `src/pages/legal/LawsuitDataPage.tsx`
- **Migration**: `supabase/migrations/20260131000000_add_lawsuit_report_columns.sql`
- **التوثيق الكامل**: `LAWSUIT_DATA_PAGE_UPDATE.md`

---

## ✅ Checklist

- [ ] تنفيذ SQL migration
- [ ] التحقق من الأعمدة في قاعدة البيانات
- [ ] اختبار عرض الصفحة
- [ ] اختبار تصدير Excel
- [ ] التحقق من البطاقات الإحصائية
- [ ] تحديث القضايا القديمة (اختياري)

---

**تم التحديث بنجاح! 🎉**

الصفحة الآن جاهزة للاستخدام مع جميع بيانات التقارير الثلاثة.
