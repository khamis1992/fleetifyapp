# تقرير مراجعة وكلاء التدقيق — Fleetify

- **التاريخ:** 26 أغسطس 2026 (توقيت الرياض)
- **النظام:** http://localhost:8080/
- **المشروع:** `C:\Users\khamis\Documents\fleetifyapp`
- **قاعدة البيانات:** Supabase `qwhunliohlkkahbspfiu` (saas)
- **الشركة:** الشركة العراف لتأجير السيارات
- **الوضع:** مراجعة فقط — لم يُغيَّر كود ولا بيانات
- **غرفة الفريق:** تدقيق وكلاء فليتيفاي

عند فتح التقرير كانت الواجهة المحلية `localhost:8080` مطفأة. صفحة الوكيل داخل النظام عند التشغيل: `/tasks?tab=system-audit` (تبويب إنجازات الوكيل).

---

## الخلاصة

هناك طبقتان منفصلتان بلا قفل مشترك:

1. **وكلاء التطبيق داخل Fleetify** (كرون + Edge Functions + جداول `system_agent_*`).
2. **وكلاء الدردشة** (Grok: موظف في النظام، موظف تدقيق، فريق عقود الأسطول، القانوني/المالي).

التدقيق الحي الفعلي هو «وكيل تدقيق النظام»: كل ليلة **3:30 فجراً** بتوقيت الرياض، وضع **تطبيق (apply)** على 7 مجالات. آخر تشغيل في 26 أغسطس أعاد نفس الرقم لثلاثة أيام متتالية: **3970 ملاحظة** (2582 مراجعة، 1207 فشل، 177 إصلاح). المخزون التاريخي: **74987** ملاحظة ما زالت `review`.

حوله نحو **10 كرونات ثانية** تلمس نفس العقود والفواتير، بلا مالك ظاهر وبلا زر إيقاف. بريد Outlook موجود في الكود وغير مجدول. تقاضي: **15** مهمة معلّقة (**14** منها `REVIEW_MISMATCH` منذ 7 أغسطس).

---

## 1) وكلاء التطبيق (الخلفية داخل النظام)

### 1.1 المحور: وكيل تدقيق النظام

| الطبقة | الدليل |
|---|---|
| الواجهة | `src/pages/tasks/TasksPage.tsx` → `SystemAuditAgentDashboard.tsx` |
| الاستدعاء | `useSystemAuditDashboard.ts` → Edge `system-audit-dashboard` |
| المنسّق (المستودع) | `supabase/functions/system-audit-orchestrator/index.ts` |
| العامل | `system-audit-worker` + `system-audit-worker-v12` |
| المنطق | `supabase/functions/_shared/system-audit/workers.ts` |
| كرون حي | `system-audit-orchestrator-v3` الساعة `30 0 * * *` UTC = **3:30 فجراً الرياض** |
| استئناف | كل 5 دقائق 0–5 UTC، ودقيقة 45 من 6–23 UTC |
| عنوان الاستدعاء الحي | `system-audit-orchestrator-v14` (اسم المجلد في المستودع ليس v14) |

**الجداول الحية**

| جدول | صفوف | الدور |
|---|---:|---|
| `system_agent_runs` | 168 | تشغيل أعلى، مصدر القدح، الملخص |
| `system_agent_jobs` | 546 | مهمة لكل شركة/مجال + قفل/مؤشر/إعادة |
| `system_agent_findings` | 110963 | ملاحظات + حالة |
| `system_agent_repairs` | 14896 | قبل/بعد + تراجع |
| `system_agent_command_registry` | 52 | أوامر الإصلاح المسموحة |

**المجالات:** عقود، محاسبة، أسطول، عملاء، مخزون، قانوني، موظفين.  
**الأوضاع:** `dry_run` / `apply`. الكرون الليلي **apply**.

**المسار:** `system_agent_create_run` → `claim_job` → دفعات العامل → `apply_*` / `rollback_*` → `finish_job`.  
النتائج تُكتب في findings/repairs، والواجهة تنشئ مهام `tasks` (`category=system_audit_review`).

الواجهة تستطيع: تحديث، إنشاء مهام مراجعة، قرار بشري على **المهمة**.  
الواجهة **لا** تستطيع: بدء أو إيقاف أو إلغاء **تشغيل/وظيفة**. أيقونة التأجيل = تأجيل قرار المهمة فقط.

**آخر تشغيل مكتمل** `cf21c3ce-…`  
بدأ 3:30 فجراً 26 أغسطس، انتهى 3:33، نحو 3.5 دقيقة، `initiated_by=null`، المصدر `cron`.  
7/7 وظائف اكتملت. ملخص 24 و25 و26 أغسطس **متطابق**: 3970 / 1207 فشل / 2582 مراجعة / 177 إصلاح — الوكيل يعيد طباعة نفس الكومة ولا يصفّي المراجعة.

`initiated_by` فارغ في **117 من 168** تشغيلاً. المالك يظهر فقط في مسار استيراد الإكسل (51).

### 1.2 وكلاء الموجة الموازية (كرون منفصل)

المساعد المشترك: `_shared/agent.ts` → `ai_agent_reviews`.  
الشركة مثبتة: `24bc0b21-4e2d-4413-9842-31719a3669f4`.

| المهمة | الرياض | ماذا تكتب |
|---|---|---|
| `daily-audit-agent` | 7:00 | فواتير/عقود/قيود + `audit_logs` |
| `nightly-ops-auditor` | 5:30 | `ai_agent_reviews` + مهام |
| `safe-auto-repair` | 6:30 | `safe_auto_repairs` (916) |
| `customer-duplicate-detector` | 7:00 (نفس ساعة daily-audit) | مقترحات دمج عملاء |
| `customer-proposal-ai-reviewer` | كل 15 د | مقترحات هوية |
| `contract-id-scanner` | كل 15 د | OCR هويات |
| `nightly-contract-terms-scan` | 6:10 | مقترحات بنود عقد |
| `smart-contract-assigner` | :00 و :30 | إعادة توزيع عقود |
| `violation-inbox-processor` | كل 15 د | مخالفات من دلو `moi-inbox` |
| `daily-contract-health-guard` | 2:15 | صحة العقود |
| `daily-legal-workflow-guard` | 5:30 (**نفس دقيقة nightly-ops**) | مهام قانونية |
| `legal-judgment-payment-matcher` | :17 كل ساعة | مطابقات سداد قانوني |
| `daily-invoice-generation` | 4:00 | توليد فواتير ناقصة |

هجرة `20260711193000_schedule_system_audit_orchestrator_v3.sql` كان يفترض أن تلغي `daily-audit-agent` — **ما زال فعّالاً**.

**مراجعون عند الطلب (صفحات، بلا كرون):**  
مراجع القيود، القضية القانونية، الإغلاق اليومي، مطابقة الدفع، رسائل التحصيل، تعبئة الهوية، مدقق التصحيح، مراجعة استيراد الإكسل.  
مجلد الواجهة: `src/components/ai-agents/*`. لا توجد صفحة محور موحّدة (الموجود اختبار `aiAgentsHub.test.ts` فقط).

### 1.3 تقاضي (سطح مكتب، ليس pg_cron)

| جدول | الحي |
|---|---|
| `taqadi_filing_jobs` | 17: **15 needs_human**، 2 ملغاة |
| `taqadi_filing_job_events` | 4177 |
| `taqadi_filing_artifacts` | 426 |
| `taqadi_automation_workers` | صف واحد، **idle** |

14 منها `REVIEW_MISMATCH` منذ **7 أغسطس**. واحدة `FINAL_APPROVAL_REQUIRED` آخر نبض 25 أغسطس 17:21. غير مربوطة بملاحظات `system_agent_findings`.

### 1.4 موجود في الكود وغير مشغّل كتدقيق

- `ingest-traffic-mail` (Microsoft Graph / Outlook) — **صفر كرون**.
- `background_jobs` — 0 صفوف (طابور ميت).
- `cto_agent_audit` / `cto_deploy_gates` — 0 صفوف.
- `pgmq` غير مثبت. `pg_cron` و `pg_net` مثبتان.
- `legal_memos` / جلسات / طعون / تنفيذ: 0.

---

## 2) وكلاء الدردشة (Grok) على نفس النظام

| الوكيل | المشكلة |
|---|---|
| **موظف في النظام** | روتين «تشغيل فليتيفاي المستمر» كل ساعة على الدقيقة 47، **ليل ونهار**. يكتب في القاعدة. وصفه فارغ. الذاكرة توقفت 22 أغسطس وهو ما زال يشغّل SQL في 26 أغسطس (نداء فشل مساء ذلك اليوم). يفضّل التعديل على التقرير فيتداخل مع فريق العقود. |
| **موظف تدقيق** | وصف فارغ، بلا روتين واضح، نفس قاعدة Fleetify. |
| **موظف تدقيق 2** | نسخة مكررة، وصف فارغ، آخر نشاط واضح 14 أغسطس. |
| **New Bot** | فارغ وغير مستخدم. |
| **فريق عقود الأسطول** (5 وكلاء) | أنهوا طابور الاستيراد/المطابقة/الإلغاء وما زالوا موجودين؛ يلمسون نفس العقود. |
| **خبير قانوني / خبير مالي** | غير مربوطين بطابور الإغلاق (عقد مراد C-ALF-0096، إثبات 7038 و8206، بريد المخالفات). |

لا يوجد صف `system_agent_runs` بمصدر `grok_bot`. لا قفل يمنع موظف في النظام وكرون الليل من «إصلاح» نفس الفاتورة/العقد.

---

## 3) المشاكل الحالية

1. **لا مالك ولا إيقاف.** 117/168 تشغيلاً بلا `initiated_by`. لا RPC إلغاء. الواجهة لا توقف الكرون.
2. **تداخل ليلي على نفس البيانات.** system-audit apply 3:30 + daily-audit 7:00 + safe-auto-repair 6:30 + توليد فواتير 4:00 + حارس قانوني وops في نفس الدقيقة 5:30.
3. **الكومة لا تُصفّى.** نفس ملخص 3970 لثلاثة أيام. 74987 ملاحظة review مقابل 253 مهمة معلّقة فقط؛ المزامنة تلغي المهام التي لم تعد في اللقطة (`canArchiveStaleTasks`) فيضيع الذيل.
4. **دفتران للإصلاح.** `system_agent_repairs` و `safe_auto_repairs` لنفس ثوابت الفاتورة/الجدول/العميل.
5. **تقاضي معلّق أسابيع** وعماله idle.
6. **Outlook غير مجدول** رغم وجود الدالة. المخالفات تعتمد رفع `moi-inbox` فقط.
7. **انحراف أسماء الإصدارات.** كرون v3، الاستدعاء الحي v14، مجلد المستودع بلا v14. لوحة التحكم تخلط تسمية 3:30 الرياض مع نافذة UTC.
8. **وكلاء دردشة بلا وصف + كاتب كل ساعة 24/7** بلا قفل مع كرون الإنتاج.
9. **أوامر قديمة في `cron.job.command` ما زالت تضم مفاتيح JWT نصاً** (الوظائف القديمة 1، 18، 23–26). الكرونات الأحدث تستخدم Vault. لا تُنسخ المفاتيح هنا.
10. **RLS معطّل** على 9 جداول منها جداول تدقيق مرحلية `_audit_*`.
11. **لا تنبيه عند الفشل.** الوظائف الفاشلة تُسجَّل في القاعدة بلا إشعار.
12. **ضعف الهوية.** أسرار مشتركة `x-agent-secret` بلا فاعل بشري واضح في أثر التدقيق.

حالة الطوابير وقت المسح: لا تشغيل `system_agent` جارٍ. العالق هو بوابة تقاضي البشرية + كومة المراجعة، لا عامل مستأجر.

---

## 4) المقترحات (لم تُنفَّذ)

### دمج الوكلاء الحاليين

| الإجراء | لماذا |
|---|---|
| طائرة تحكم واحدة: `system_agent_*` | إيجار واحد، مالك، إيقاف، سجل واحد |
| إيقاف أو لفّ `daily-audit-agent` و`nightly-ops` و`safe-auto-repair` كعمّال تحت `system_agent_jobs` | إلغاء التكرار الليلي |
| إبعاد `daily-legal-workflow-guard` عن نفس دقيقة `nightly-ops` | تصادم 5:30 |
| كاتب دردشة واحد فقط: `موظف في النظام` | بساعات دوام، وصف واضح، وقفل: يكتب صف `system_agent_runs` (`trigger_source=grok_bot`) قبل أي مالية/قانوني |
| دمج `موظف تدقيق` + `موظف تدقيق 2` في مدقّق قراءة فقط + ملخص يومي | نفس القاعدة بلا فائدة من الاثنين |
| إيقاف فريق عقود الأسطول إلا عند ملف سطح مكتب جديد | الطابور انتهى |
| ربط القانوني/المالي عند القضية فقط | لا تشغيل حر |
| إخفاء أو حذف `New Bot` | بلا دور |

### وكلاء أو تكامل جديد (بعد الموافقة)

1. **مالك + إيقاف في الواجهة:** `initiated_by`، RPC إلغاء، مفتاح قتل للكرونات 38/40/41.
2. **وكيل بريد المخالفات:** جدولة `ingest-traffic-mail` (مجلد Traffic Fines) ثم ربط فريد لوحة/تاريخ.
3. **بوابة تقاضي:** ربط `needs_human` بمهام `system_audit_review`، فك `REVIEW_MISMATCH`، لا تشغيل موازٍ مع حارس القانوني.
4. **طي ماسحات العقود** (بنود + هوية) داخل عامل مجال العقود بمفتاح `dedupe_key`.
5. **تنظيف أمني:** تدوير المفاتيح من أوامر الكرون القديمة؛ توثيق/نشر `orchestrator-v14` مقابل المستودع.

---

## 5) قرارات تنتظر خميس (خارج الوكلاء، ما زالت مفتوحة)

- عقد موقّع لمراد المسعودي **C-ALF-0096** ثم إعادة تقاضي.
- إثبات مخالفات للعقود **7038** و**8206** ثم التحويل القانوني.
- خمسة عقود شغّالة/قانونية بلا نسخة موقّعة كانت معلّقة سابقاً (منها C-ALF-0006، C-ALF-0022، C-ALF-0079).
- تطبيق قالب المذكرة بلا إنذار في مولّد النظام: ينتظر موافقة صريحة.

---

## 6) الفريق الذي رافق المراجعة

غرفة **تدقيق وكلاء فليتيفاي**: معمار وكود فليتيفاي، أوركسترا وكلاء فليتيفاي، أمن وبيانات فليتيفاي، تشغيل وتدقيق فليتيفاي، تكامل وكلاء فليتيفاي.

لم يُفتح طلب سحب ولم يُمس الكود. التنفيذ يبدأ فقط إذا وافقت على توحيد التحكم.
