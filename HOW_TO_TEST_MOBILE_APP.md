# كيفية اختبار تطبيق الجوال

## 🚀 الطريقة السريعة

### 1. بناء التطبيق
```bash
npm run build:ci
npx cap sync android
```

### 2. فتح في Android Studio
```bash
npx cap open android
```

### 3. تشغيل التطبيق
- اضغط على زر ▶️ (Run) في Android Studio
- أو اختر جهاز/محاكي من القائمة

## 📱 اختبار تسجيل الدخول

### بيانات الاختبار
```
البريد الإلكتروني: [أدخل بريد موجود في النظام]
كلمة المرور: [كلمة المرور الصحيحة]
```

### السيناريوهات

#### ✅ سيناريو 1: تسجيل دخول ناجح
1. افتح التطبيق
2. أدخل بيانات صحيحة
3. اضغط "تسجيل الدخول"
4. **النتيجة**: الانتقال للصفحة الرئيسية خلال 2-3 ثواني

#### ❌ سيناريو 2: بيانات خاطئة
1. أدخل بريد أو كلمة مرور خاطئة
2. اضغط "تسجيل الدخول"
3. **النتيجة**: رسالة خطأ بالعربية

#### 🔧 سيناريو 3: التشخيص
1. من شاشة تسجيل الدخول
2. اضغط "🔧 تشخيص المشاكل" أسفل الصفحة
3. **النتيجة**: صفحة فحص شاملة

## 🔍 قراءة نتائج التشخيص

### الرموز
- ✅ **أخضر**: يعمل بشكل صحيح
- ⚠️ **أصفر**: تحذير (قد يعمل لكن يحتاج انتباه)
- ❌ **أحمر**: خطأ (يحتاج إصلاح فوري)
- 🔄 **رمادي**: جاري الفحص...

### الفحوصات
1. **platform**: نوع المنصة (Android/iOS/Web)
2. **config**: إعدادات Supabase
3. **network**: الاتصال بالإنترنت
4. **supabase**: الاتصال بقاعدة البيانات
5. **auth-session**: جلسة المصادقة
6. **storage**: التخزين المحلي
7. **auth-context**: حالة المستخدم

## 🐛 حل المشاكل الشائعة

### المشكلة: "يبقى في شاشة التحميل"
**الحل**:
1. افتح `/mobile/diagnostics`
2. تحقق من:
   - ✅ network (الإنترنت)
   - ✅ supabase (قاعدة البيانات)
   - ✅ auth-session (الجلسة)

### المشكلة: "خطأ في الاتصال"
**الحل**:
1. تحقق من اتصال الإنترنت
2. تحقق من إعدادات Supabase في `.env`
3. أعد بناء التطبيق: `npm run build:ci && npx cap sync android`

### المشكلة: "صفحة بيضاء"
**الحل**:
1. افتح Chrome DevTools: `chrome://inspect`
2. ابحث عن أخطاء JavaScript
3. تحقق من أن `dist` folder محدث

## 📊 مراقبة Logs

### في Android Studio
1. افتح **Logcat** (أسفل الشاشة)
2. ابحث عن:
   - `[MobileLogin]` - logs تسجيل الدخول
   - `[AUTH_CONTEXT]` - logs المصادقة
   - `[CapacitorStorage]` - logs التخزين

### في Chrome DevTools
1. افتح `chrome://inspect`
2. اختر جهازك
3. اضغط **inspect**
4. افتح **Console**

## 🔄 إعادة البناء بعد التعديلات

```bash
# 1. بناء المشروع
npm run build:ci

# 2. مزامنة مع Android
npx cap sync android

# 3. فتح في Android Studio (اختياري)
npx cap open android
```

## 📦 بناء APK للتوزيع

```bash
# في Android Studio:
# Build > Build Bundle(s) / APK(s) > Build APK(s)

# أو من سطر الأوامر:
cd android
./gradlew assembleDebug

# الملف سيكون في:
# android/app/build/outputs/apk/debug/app-debug.apk
```

## 💡 نصائح

1. **استخدم محاكي سريع**: Android 11+ مع Google Play
2. **فعّل USB Debugging**: في إعدادات المطور
3. **راقب الـ Logs**: دائماً افتح Logcat
4. **اختبر على جهاز حقيقي**: للتأكد من الأداء

## 📞 الدعم

إذا واجهت مشكلة:
1. افتح `/mobile/diagnostics`
2. التقط screenshot
3. تحقق من Logs في Logcat
4. شارك المعلومات مع فريق التطوير

---

**آخر تحديث**: 2026-02-01
