# 📋 صفحة تسجيل إجراءات الدفعات - FleetifyAI

## نظرة عامة
صفحة ذكية لتسجيل ومتابعة الدفعات مع نظام ذكاء اصطناعي متقدم يفهم النصوص العربية الطبيعية.

---

## ✨ المميزات الرئيسية

### 1. **عرض العقود النشطة فقط**
- ✅ يتم عرض أسماء العملاء الذين لديهم عقود نشطة فقط
- ✅ قائمة منسدلة ذكية تعرض: اسم العميل - رقم المركبة - رقم العقد
- ✅ ملء تلقائي لجميع التفاصيل عند اختيار العميل

### 2. **الملء التلقائي للبيانات**
عند اختيار عميل من القائمة، يتم ملء:
- رقم المركبة
- لون المركبة
- رقم الجوال
- القسط الشهري
- معلومات العقد

### 3. **نظام الذكاء الاصطناعي (FleetifyAI)**
يفهم النصوص العربية في حقل الملاحظات مثل:
- "تم سداد مبلغ 1500" → يستخرج المبلغ ونوع العملية
- "دفع 2000 نقداً" → يستخرج المبلغ وطريقة الدفع (نقدي)
- "تحويل بنكي 3500" → يستخرج المبلغ وطريقة الدفع (بنكي)
- "دفعة متأخرة 1000 مع غرامة 100" → يستخرج المبلغ والغرامة
- "سداد كامل 15000" → يحدد نوع العملية كسداد كامل

### 4. **البحث والتصفية**
- 🔍 بحث فوري في جميع الحقول (اسم العميل، رقم المركبة، رقم الجوال، الملاحظات)
- تصفية حسب الحالة (الكل، المسددة، المعلقة)

### 5. **إحصائيات تلقائية**
- إجمالي عدد الدفعات
- إجمالي المبالغ المسجلة
- متوسط القسط الشهري

### 6. **واجهة Excel-like**
- جدول تفاعلي مع إمكانية التعديل المباشر
- تلوين تلقائي للصفوف
- تأثيرات بصرية عند التفاعل

---

## 🚀 كيفية الاستخدام

### الطريقة الأولى: استخدام مباشر (Demo)
1. افتح ملف `payment_sheet_1.html` في المتصفح
2. ستجد 8 عقود نشطة تجريبية جاهزة
3. اضغط "إضافة دفعة"
4. اختر عميلاً من القائمة المنسدلة
5. سيتم ملء جميع التفاصيل تلقائياً
6. اكتب ملاحظة ذكية مثل: "تم سداد مبلغ 1500"
7. سيظهر نظام AI نافذة تأكيد مع جميع التفاصيل المستخرجة

### الطريقة الثانية: دمج مع FleetifyApp

#### 1. جلب العقود النشطة
في الملف `payment_sheet_1.html`، ابحث عن دالة `fetchActiveContracts()` وقم بإلغاء التعليق على الكود الحقيقي:

```javascript
async function fetchActiveContracts() {
    try {
        const response = await fetch('/api/rental-agreements', {
            method: 'GET',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${localStorage.getItem('token')}`
            }
        });
        
        if (response.ok) {
            const agreements = await response.json();
            // تصفية العقود النشطة فقط
            activeContracts = agreements.filter(agreement => 
                agreement.status === 'active' || agreement.status === 'approved'
            ).map(agreement => ({
                contractId: agreement.agreement_number || agreement.id,
                customerId: agreement.customer_id,
                customerName: agreement.customer_name,
                phone: agreement.customer_phone,
                vehicleId: agreement.vehicle_id,
                vehicleNumber: agreement.vehicle_plate_number || agreement.vehicle_number,
                color: agreement.vehicle_color || 'white',
                monthlyPayment: agreement.monthly_payment || agreement.rental_rate,
                startDate: agreement.start_date,
                endDate: agreement.end_date,
                status: agreement.status
            }));
        }
    } catch (error) {
        console.error('خطأ في جلب العقود:', error);
        showToast('error', 'فشل في جلب العقود النشطة');
    }
}
```

#### 2. حفظ الدفعات
في دالة `saveData()`، قم بإلغاء التعليق على الكود الحقيقي:

```javascript
function saveData() {
    const savePromises = paymentsData.map(async (payment) => {
        const paymentData = {
            contract_id: payment.contractId,
            customer_id: payment.customerId,
            vehicle_id: payment.vehicleId,
            amount: payment.monthlyPayment,
            payment_date: new Date().toISOString(),
            notes: payment.notes,
            payment_method: 'manual',
            status: 'completed'
        };
        
        const response = await fetch('/api/payments', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${localStorage.getItem('token')}`
            },
            body: JSON.stringify(paymentData)
        });
        
        return response.json();
    });
    
    Promise.all(savePromises)
        .then(results => {
            showToast('success', `تم حفظ ${results.length} دفعة بنجاح!`);
            paymentsData = [];
            renderTable();
        })
        .catch(error => {
            showToast('error', 'فشل في حفظ بعض الدفعات');
        });
}
```

#### 3. دمج نظام AI مع OpenAI
لتفعيل نظام AI الحقيقي، أضف هذا الكود:

```javascript
async function performAIAnalysis(paymentId, text, payment) {
    try {
        const response = await fetch('/api/ai/analyze-payment', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${localStorage.getItem('token')}`
            },
            body: JSON.stringify({
                text: text,
                customerId: payment.customerId,
                contractId: payment.contractId,
                monthlyPayment: payment.monthlyPayment
            })
        });
        
        if (response.ok) {
            const analysis = await response.json();
            showAIDetectionBox(payment, analysis);
        }
    } catch (error) {
        console.error('خطأ في تحليل AI:', error);
    }
}
```

---

## 📁 بنية الملفات

```
.superdesign/design_iterations/
├── payment_sheet_theme.css      # نظام الألوان والخطوط
├── payment_sheet_1.html          # الصفحة الرئيسية
└── payment_sheet_README.md       # هذا الملف (التوثيق)
```

---

## 🎨 التخصيص

### تغيير الألوان
افتح ملف `payment_sheet_theme.css` وعدل المتغيرات:

```css
:root {
  --primary: oklch(0.5500 0.1600 160);        /* الأخضر الأساسي */
  --accent: oklch(0.6800 0.1900 50);          /* البرتقالي للـ AI */
  --success: oklch(0.6000 0.1700 145);        /* الأخضر للنجاح */
  /* ... باقي الألوان */
}
```

### إضافة ألوان مركبات جديدة
في ملف `payment_sheet_1.html`:

```javascript
const vehicleColors = {
    'white': { name: 'أبيض', hex: '#ffffff', border: '#e5e7eb' },
    'black': { name: 'أسود', hex: '#000000' },
    // أضف لون جديد:
    'yellow': { name: 'أصفر', hex: '#fbbf24' }
};
```

---

## 🔧 متطلبات التشغيل

### للاستخدام المباشر (Demo):
- متصفح حديث (Chrome, Firefox, Safari, Edge)
- لا يتطلب سيرفر

### للدمج مع FleetifyApp:
- Node.js 18+
- قاعدة بيانات PostgreSQL/Supabase
- API endpoints للعقود والدفعات
- مفتاح OpenAI API (اختياري للـ AI الحقيقي)

---

## 🌟 نصائح للاستخدام الأمثل

1. **استخدم الاختصارات:**
   - اضغط Tab للانتقال بين الحقول
   - استخدم Enter في حقل الملاحظات لتشغيل التحليل الفوري

2. **الملاحظات الذكية:**
   - اكتب بشكل طبيعي وواضح
   - يمكنك استخدام الأرقام العربية أو الإنجليزية
   - النظام يفهم عدة صيغ للمبالغ (1500، 1,500، ١٥٠٠)

3. **البحث السريع:**
   - ابحث برقم المركبة للعثور على دفعة محددة
   - ابحث باسم العميل لرؤية جميع دفعاته
   - ابحث برقم الجوال للتحقق من الدفعات

4. **التصدير:**
   - استخدم زر "تصدير Excel" لحفظ نسخة من البيانات
   - استخدم زر "طباعة" للحصول على نسخة ورقية

---

## 🐛 حل المشاكل الشائعة

### المشكلة: لا تظهر العقود النشطة
**الحل:**
- تأكد من تشغيل السيرفر
- تحقق من صحة Token في localStorage
- تأكد من أن API endpoint صحيح

### المشكلة: AI لا يكتشف الدفعات
**الحل:**
- تأكد من كتابة مبلغ واضح في الملاحظات
- انتظر 1.5 ثانية بعد التوقف عن الكتابة
- جرب صيغة مختلفة (مثال: "دفع 1500" بدلاً من "1500")

### المشكلة: البيانات لا تُحفظ
**الحل:**
- تأكد من اختيار عميل لكل دفعة
- تأكد من كتابة ملاحظات
- تحقق من اتصال الإنترنت
- افتح console للتحقق من الأخطاء

---

## 📞 الدعم والمساعدة

- للأسئلة التقنية: افتح console وابحث عن رسائل الخطأ
- للاقتراحات: قم بتعديل الكود مباشرة (مفتوح المصدر)
- للتوثيق الإضافي: راجع التعليقات داخل ملف HTML

---

## 🎯 خطط مستقبلية

- [ ] إضافة دعم للدفعات الجزئية
- [ ] نظام إشعارات للدفعات المتأخرة
- [ ] تقارير تفصيلية وإحصائيات متقدمة
- [ ] دعم العملات المتعددة
- [ ] تطبيق جوال (PWA)
- [ ] تكامل مع أنظمة الدفع الإلكتروني

---

## 📝 سجل التحديثات

### الإصدار 1.0 (2025-11-03)
- ✅ عرض العقود النشطة فقط
- ✅ ملء تلقائي للبيانات عند اختيار العميل
- ✅ نظام AI ذكي لفهم النصوص العربية
- ✅ بحث وتصفية متقدمة
- ✅ واجهة Excel-like تفاعلية
- ✅ إحصائيات فورية
- ✅ تصميم متجاوب للجوال

---

## 📄 الترخيص

هذا التصميم تم إنشاؤه بواسطة **SuperDesign AI** لمشروع **FleetifyApp**.
يمكنك استخدامه وتعديله بحرية.

---

## 🙏 شكر خاص

تم التصميم باستخدام:
- **Tailwind CSS** للتصميم السريع
- **Lucide Icons** للأيقونات الجميلة
- **Google Fonts** (Tajawal & JetBrains Mono)
- **FleetifyAI Engine** لنظام الذكاء الاصطناعي

---

**تم التصميم بواسطة SuperDesign AI 🎨✨**

