# 📋 FleetifyApp - تسجيل الدفعات (الإصدار 2)

## 🎯 نظرة عامة
صفحة ذكية لتسجيل الدفعات تعرض جميع العملاء ذوي العقود النشطة مباشرةً في جدول واحد، مع نظام ذكاء اصطناعي لفهم النصوص العربية.

---

## ✨ المميزات الرئيسية

### 1. **عرض جميع العقود النشطة**
- ✅ لا حاجة لإضافة صفوف - جميع العملاء معروضين مباشرة
- ✅ كل عقد نشط له صف واحد في الجدول
- ✅ البيانات الأساسية معروضة للقراءة فقط

### 2. **حقل واحد للتعديل**
- 📝 حقل "تسجيل الدفعة" فقط قابل للتعديل
- 🤖 نظام AI يحلل النص تلقائياً
- ✓ زر تأكيد لكل دفعة

### 3. **تتبع الحالة**
- ⏳ "في الانتظار" - لم يتم التسجيل بعد
- ✓ "مسددة" - تم تسجيل الدفعة

### 4. **ألوان النظام**
- 🔴 الأحمر الأساسي `hsl(0, 70%, 45%)`
- 🟢 الأخضر للنجاح `hsl(142, 56%, 42%)`
- 🟠 البرتقالي للتنبيهات `hsl(25, 90%, 55%)`

---

## 🚀 كيفية الاستخدام

### الخطوات البسيطة:

1. **افتح الصفحة**
   - افتح `payment_sheet_2.html` في المتصفح
   - ستجد 8 عقود نشطة معروضة

2. **ابحث عن العميل**
   - استخدم مربع البحث للعثور على عميل محدد
   - البحث يعمل على: الاسم، المركبة، رقم الجوال

3. **سجل الدفعة**
   - في حقل "تسجيل الدفعة" اكتب مثلاً: "تم سداد مبلغ 1500"
   - سيظهر نظام AI نافذة بالتفاصيل المستخرجة
   - اضغط "تأكيد" لتسجيل الدفعة

4. **تتبع الحالة**
   - الحالة تتغير تلقائياً من "في الانتظار" إلى "مسددة"
   - الإحصائيات تتحدث فوراً

5. **احفظ البيانات**
   - اضغط "حفظ الدفعات" في الأعلى
   - يتم حفظ جميع الدفعات المسجلة

---

## 📊 الإحصائيات

يتم عرض 3 إحصائيات رئيسية:

1. **عقود نشطة** 🔵
   - إجمالي العقود المعروضة

2. **دفعات مسجلة** 🟢
   - عدد الدفعات التي تم تأكيدها

3. **في الانتظار** 🟠
   - عدد العقود التي لم يتم تسجيل دفعة لها بعد

---

## 🎨 التصميم

### الألوان المستخدمة:

| العنصر | اللون | الاستخدام |
|--------|-------|----------|
| Primary | `hsl(0, 70%, 45%)` | الأزرار الرئيسية، العناوين |
| Success | `hsl(142, 56%, 42%)` | الدفعات المسددة |
| Warning | `hsl(25, 90%, 55%)` | AI والتنبيهات |
| Background | `hsl(0, 0%, 96%)` | خلفية الصفحة |
| Card | `white` | البطاقات والجدول |

### الخطوط:
- **العربية:** Tajawal (ممتاز للقراءة)
- **الأرقام:** JetBrains Mono (واضح ومميز)

---

## 🤖 نظام الذكاء الاصطناعي

### ما يفهمه النظام:

| الجملة | النتيجة |
|--------|----------|
| "تم سداد مبلغ 1500" | مبلغ: 1500 ريال |
| "دفع 2000 نقداً" | مبلغ: 2000، طريقة: نقدي |
| "تحويل بنكي 3500" | مبلغ: 3500، طريقة: بنكي |
| "دفعة 1000 مع غرامة 100" | مبلغ: 1000، غرامة: 100 |

### كيف يعمل:
1. تكتب في حقل الملاحظات
2. ينتظر 1.5 ثانية بعد التوقف عن الكتابة
3. يحلل النص ويستخرج المعلومات
4. يعرض نافذة بالتفاصيل المكتشفة

---

## 🔌 الدمج مع FleetifyApp

### 1. جلب العقود النشطة

استبدل البيانات التجريبية بـ API حقيقي:

```javascript
// في بداية ملف payment_sheet_2.html
async function fetchActiveContracts() {
    try {
        const response = await fetch('/api/rental-agreements', {
            method: 'GET',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${localStorage.getItem('token')}`
            }
        });
        
        if (response.ok) {
            const agreements = await response.json();
            activeContracts = agreements
                .filter(a => a.status === 'active')
                .map(a => ({
                    contractId: a.agreement_number,
                    customerId: a.customer_id,
                    customerName: a.customer_name,
                    phone: a.customer_phone,
                    vehicleNumber: a.vehicle_plate_number,
                    color: a.vehicle_color,
                    monthlyPayment: a.monthly_payment,
                    notes: '',
                    status: 'pending'
                }));
            
            renderTable();
        }
    } catch (error) {
        console.error('خطأ في جلب العقود:', error);
    }
}

// استدعاء الدالة عند تحميل الصفحة
document.addEventListener('DOMContentLoaded', async () => {
    await fetchActiveContracts();
});
```

### 2. حفظ الدفعات

في دالة `saveAllPayments()`:

```javascript
async function saveAllPayments() {
    const payments = activeContracts.filter(c => c.status === 'paid' && c.notes);
    
    if (payments.length === 0) {
        showToast('error', 'لا توجد دفعات لحفظها!');
        return;
    }
    
    try {
        const promises = payments.map(async (payment) => {
            const response = await fetch('/api/payments', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${localStorage.getItem('token')}`
                },
                body: JSON.stringify({
                    contract_id: payment.contractId,
                    customer_id: payment.customerId,
                    amount: payment.monthlyPayment,
                    notes: payment.notes,
                    payment_date: new Date().toISOString(),
                    status: 'completed'
                })
            });
            
            return response.json();
        });
        
        await Promise.all(promises);
        showToast('success', `تم حفظ ${payments.length} دفعة بنجاح!`);
    } catch (error) {
        console.error('خطأ في حفظ الدفعات:', error);
        showToast('error', 'فشل في حفظ بعض الدفعات');
    }
}
```

---

## 📱 التصميم المتجاوب

الصفحة متجاوبة بالكامل:
- **Desktop:** جدول كامل مع جميع الأعمدة
- **Tablet:** جدول قابل للتمرير أفقياً
- **Mobile:** جدول مصغر مع تمرير

---

## 🎯 التحسينات المستقبلية

- [ ] إضافة فلترة حسب الحالة (مسددة/معلقة)
- [ ] إضافة ترتيب حسب الأعمدة
- [ ] إضافة تصدير PDF
- [ ] إضافة طباعة
- [ ] إضافة تاريخ الدفعة
- [ ] إضافة ملفات مرفقة

---

## 🐛 حل المشاكل

### المشكلة: لا تظهر العقود
**الحل:** تحقق من:
- اتصال الإنترنت
- صحة الـ API endpoint
- Token في localStorage

### المشكلة: AI لا يكتشف المبلغ
**الحل:** 
- اكتب المبلغ بوضوح
- استخدم أرقام إنجليزية (1500)
- جرب صيغة مختلفة

### المشكلة: زر التأكيد معطل
**الحل:**
- تأكد من كتابة شيء في حقل الملاحظات
- الزر يعمل فقط عند وجود نص

---

## 📄 الملفات

```
.superdesign/design_iterations/
├── payment_sheet_1.html          # الإصدار القديم
├── payment_sheet_2.html          # الإصدار الجديد ⭐
├── payment_sheet_theme.css       # ملف الثيم (اختياري)
└── payment_sheet_v2_README.md    # هذا الملف
```

---

## ✅ الفروقات عن الإصدار السابق

| الميزة | الإصدار 1 | الإصدار 2 |
|--------|-----------|-----------|
| عرض العملاء | اختيار من قائمة | جميع العقود معروضة |
| إضافة صفوف | يدوي | غير مطلوب |
| الحقول القابلة للتعديل | 6 حقول | حقل واحد فقط |
| الألوان | أخضر أساسي | أحمر النظام |
| البساطة | متوسط | عالية جداً ⭐ |

---

**تم التصميم بواسطة SuperDesign AI 🎨✨**  
**للاستخدام في FleetifyApp**

تاريخ الإنشاء: 2025-11-03  
الإصدار: 2.0

