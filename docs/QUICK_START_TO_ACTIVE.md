# 🚀 دليل سريع: تحديث عقود العراف إلى "نشطة"

## المهمة
تحويل **101 عقد** من حالة "قيد المراجعة" إلى "نشطة" في شركة العراف

---

## ⚡ الحل الأسرع (موصى به)

### استخدم Supabase Dashboard:

1. **افتح**: https://supabase.com/dashboard
2. **اختر**: مشروع Fleetify
3. **اذهب إلى**: SQL Editor → New Query
4. **انسخ والصق**:

```sql
-- نفذ هذا الاستعلام 3 مرات (كل مرة اضغط Run)

UPDATE contracts
SET status = 'active', updated_at = NOW()
WHERE id IN (
    SELECT id FROM contracts 
    WHERE company_id = '24bc0b21-4e2d-4413-9842-31719a3669f4'
      AND status = 'under_review'
    LIMIT 50
);

-- بعد كل تنفيذ، شاهد العدد المتبقي:
SELECT COUNT(*) as remaining FROM contracts
WHERE company_id = '24bc0b21-4e2d-4413-9842-31719a3669f4'
  AND status = 'under_review';
```

5. **كرر** الضغط على Run حتى يصبح المتبقي = 0

---

## 📊 التنفيذ خطوة بخطوة

### المرة الأولى:
- اضغط Run
- النتيجة: تم تحديث 50 عقد
- المتبقي: 51 عقد

### المرة الثانية:
- اضغط Run مرة أخرى
- النتيجة: تم تحديث 50 عقد
- المتبقي: 1 عقد

### المرة الثالثة:
- اضغط Run مرة أخيرة
- النتيجة: تم تحديث 1 عقد
- المتبقي: 0 عقد ✅

---

## ✅ التحقق من النجاح

بعد الانتهاء، نفذ هذا للتأكد:

```sql
SELECT 
    status,
    COUNT(*) as count
FROM contracts
WHERE company_id = '24bc0b21-4e2d-4413-9842-31719a3669f4'
GROUP BY status
ORDER BY count DESC;
```

يجب أن ترى:
- **تحت المراجعة**: 0 ✅
- **النشطة**: زيادة بـ 101 عقد ✅

---

## 🎯 ملخص

| الخطوة | الإجراء | النتيجة |
|--------|---------|---------|
| 1 | افتح Supabase Dashboard | - |
| 2 | انسخ السكريبت أعلاه | - |
| 3 | Run × 3 مرات | 101 عقد محدث |
| 4 | تحقق من النتيجة | 0 متبقي |

---

**الوقت الكلي**: 2-3 دقائق  
**الصعوبة**: سهل جداً ⭐  
**النجاح**: مضمون 100% ✅
