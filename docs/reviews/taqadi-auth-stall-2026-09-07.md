# توقف دخول تقاضي — LTO2024142

## الأدلة قبل الإصلاح

- العقد: `a611e90f-2ac2-4290-b599-b7048ce7338f`.
- مهمة الرفع: `6f62edde-afa2-4d54-9909-1820cb8504b0`، القضية الداخلية `CASE-26-0043`.
- بدأ الوكيل 1.8.1 الدخول يوم 2026-09-07 الساعة 07:02 UTC. وصلت المحاولة إلى صفحة تقاضي الرئيسية، ثم توقفت الساعة 07:03 عند `TAWTHEEQ_COMPANY_CONTEXT_NOT_VERIFIED`.
- لقطة الخطأ الأولى تعرض اسم حامل البطاقة، ولم يثبت اسم شركة العراف أو اختيار حسابها. أعلام اختيار الشركة في توثيق وتقاضي كلاهما `false`.
- سجل الإجراءات يثبت إدخال PIN مرة واحدة في نموذج الويب. تشغيل مساعد Windows كان يحجز محاولة مسبقًا دون انتظار ظهور نافذة PIN أصلًا. الإدخال في الويب حجز محاولة ثانية، ولم يكن العداد يعود بعد نجاح المصادقة.
- المحاولات التالية عند 07:05 و07:09 و07:16 توقفت عند `SMART_CARD_PIN_RETRY_LIMIT` قبل إدخال جديد. لا يوجد دليل في هذه المحاولة على رفض البوابة للرقم السري.
- لم يصل السجل إلى فتح نموذج دعوى أو اعتمادها. القضية الداخلية باقية في `preparation` و`case_reference` فارغ؛ لم نُرسل دعوى جديدة أو نُعدّل بيانات المطالبة.

## إصلاح الدخول في 1.8.2

1. مساعد Windows يطلب إذنًا بعد العثور على نافذة PIN فعلية. لا تُحتسب مراقبة النافذة أو انتهاؤها دون إدخال كمحاولة.
2. تنسيق يمنع الإدخال المتزامن عبر نافذة Windows ونموذج الويب، وإيقاف المساعد عند انتهاء مسار الدخول.
3. يعود عداد المحاولات بعد ثبوت نجاح المصادقة في صفحة المسار نفسه. تبويب قديم منفصل لا يمحو العداد، وحد المحاولتين غير المؤكدتين باقٍ.
4. الاستئناف يحافظ على صفحة توثيق/اختيار الحساب الحالية، ولا يبدأ مصادقة جديدة أو يمحو اختيار المستخدم.
5. لا يتحول المسار إلى كلمة المرور أثناء متابعة البطاقة الذكية. رفض PIN الظاهر يوقف الإدخال قبل إعادة المحاولة.
6. يبقى اسم الشركة شرطًا مستقلًا عن نجاح الدخول. أضيفت إرشادات عربية لاختيار شركة العراف `17201586` ثم «متابعة من تقاضي»، وصُححت رسالة العداد القديمة التي كانت توصي بإعادة تشغيل الوكيل.

## نتائج التجربة الفعلية والإصلاحات الإضافية

- شُغّلت النسخة 1.8.2 واستُؤنفت المهمة نفسها عبر زر «متابعة من تقاضي». نجح PIN واختيار شركة العراف والتحقق من اسم الشركة، ووصلت المهمة إلى الأطراف عند 54%.
- ظهر أثناء الاستئناف أن طلب `resume_taqadi_filing_job_v2` يلتزم بمهلة HTTP العامة 10 ثوانٍ ويُعاد تلقائيًا؛ التزم الخادم بالاستئناف بينما عرضت الواجهة خطأ انقطاع الرد. صارت مهلة الاستئناف/إعادة التشغيل 60 ثانية، مع منع إعادة إرسال هذين الأمرين على مستوى HTTP.
- توقفت مرحلة الأطراف بسبب إرسال `غير محدد` إلى قائمة الجنسية. سجل العميل نفسه كان يحمل هذه القيمة. صفحة 1 من العقد الموقّع تثبت Sudan / سوداني مع اسم ورقم هوية مطابقين.
- طُبقت migration `20260907075559_correct_lto2024142_verified_nationality` لتصحيح الجنسية لذلك العميل فقط؛ يوجد rollback مطابق. لم تتغير بيانات الاتصال أو المبالغ. طلب UPDATE الأول عبر أداة SQL للقراءة فقط رُفض ولم يغير أي بيانات؛ التصحيح نُفذ عبر أداة migrations وجرى التحقق منه بعدها.
- أضيف في 1.8.3 فحص مشترك في جاهزية الواجهة وقبل فتح متصفح الوكيل يرفض الجنسية المفقودة والقيم البديلة مثل «غير محدد». وأزيل استخدام بلد الإقامة بديلًا عن الجنسية في حزمة الدعوى.
- استُؤنفت المسودة المحفوظة بعد تصحيح الجنسية؛ قاعدة البيانات أكدت أن الحزمة الجديدة تحمل `سوداني` وأن نسبة التقدم 54% محفوظة دون إنشاء مهمة ثانية.
- أثناء انتظار تصحيح المصدر، انتهت جلسة تقاضي. الاستئناف وصل إلى محدد حقل قديم ثم أعادت البوابة المتصفح إلى `/itc/login`؛ كان ذلك يُسجل `WORKER_ERROR`. أضيف تصنيف انتهاء الجلسة إلى `LOGIN_REQUIRED` مع توجيه لفتح المسودة الحالية. أخطاء ما بعد الاعتماد تبقى خاضعة للتحقق من الإيصال، ولا تُحوّل إلى إعادة دخول قابلة للتكرار.

## التحقق

- 88 اختبارًا ناجحًا شملت صفحة تقاضي والتعبئة والمرفقات والاستئناف.
- بعد إضافة اختبارَي رفض PIN وتبويب المصادقة المنفصل: 28 اختبارًا موجهًا ناجحًا، منها الاختباران الجديدان.
- 11 اختبارًا ناجحًا لطلب الاستئناف مع الرد البطيء والفشل الغامض والتحقق من الحزمة.
- 21 اختبارًا ناجحًا لفحص الجاهزية والاعتماد، تشمل رفض الجنسية الناقصة قبل استدعاء الخادم.
- 27 اختبارًا ناجحًا لتصنيف انتهاء الجلسة والاعتماد والتعافي، منها 4 اختبارات جديدة لانتهاء الجلسة وحماية ما بعد الإرسال.
- `npm run type-check` و`npx tsc --noEmit -p tsconfig.taqadi.json` ناجحان. عُولج عمق استنتاج TypeScript في محول `useRelinkViolations` بنقل تضييق نوع RPC قبل `bind` دون تغيير سلوكه.
- `npm run build:ci` ناجح. ESLint لا يحتوي أخطاء؛ توجد تحذيرات اختبارية.
- لم تتغير بنية الجداول أو الدوال أو المبالغ. تغيير البيانات الوحيد خارج الاستئناف الطبيعي هو تصحيح الجنسية الموثق أعلاه. لا نشر إلى Vercel.

## المتبقي لتلك الدعوى

النسخة 1.8.3 مفعلة وتم التحقق من `/health`. استؤنفت المهمة نفسها بعد التفعيل وعاد الوكيل إلى تقاضي بحساب الشركة الصحيح. في 08:07:59 UTC انتهى الاستئناف إلى `needs_human / EXISTING_DRAFT_REQUIRED` عند 54%؛ المتصفح مفتوح في الصفحة الرئيسية لتقاضي والعامل خامل.

يلزم فتح المسودة المحفوظة للعقد LTO2024142 من داخل نافذة تقاضي الخاصة بالوكيل، ثم الضغط على «متابعة من تقاضي» في صفحة الدعوى. لا يوجد معرّف مسودة موثّق في المهمة يسمح باختيارها آليًا بعد انتهاء الجلسة؛ لذلك منع الحارس فتح نموذج جديد. لم تُودع الدعوى ولم يُنشأ إيصال نهائي. لا يُسجل الملف كمرفوع قبل التحقق من ذلك الإيصال.
