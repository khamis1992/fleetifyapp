# 📖 دليل شامل: كيف يعمل النظام من الألف إلى الياء

## 📋 المحتويات

1. [إرسال رسائل الواتساب](#1-إرسال-رسائل-الواتساب)
2. [تحويل العملاء لقسم القضايا](#2-تحويل-العملاء-لقسم-القضايا)
3. [تسجيل الدفعات عند قدوم العملاء](#3-تسجيل-الدفعات-عند-قدوم-العملاء)

---

## 1️⃣ إرسال رسائل الواتساب

### 🔄 العملية التلقائية

#### الخطوة 1: إنشاء الفواتير (يوم 28 من كل شهر)
```
يوم 28 من الشهر (9:00 صباحاً)
    ↓
Cron Job: run_monthly_invoice_generation()
    ↓
إنشاء فواتير لجميع العقود النشطة للشهر القادم
    ↓
تاريخ الفاتورة: يوم 1 من الشهر القادم
تاريخ الاستحقاق: يوم 1 من الشهر القادم
```

#### الخطوة 2: جدولة التذكيرات (تلقائياً عند إنشاء الفاتورة)
عند إنشاء أي فاتورة جديدة، يتم إنشاء **4 تذكيرات تلقائياً**:

| المرحلة | التوقيت | نوع الرسالة | الهدف |
|---------|---------|-------------|-------|
| **1** | قبل 3 أيام من الاستحقاق | ودية | تنبيه مبكر |
| **2** | يوم الاستحقاق (يوم 1) | مهذبة | تذكير بالدفع |
| **3** | بعد 3 أيام من الاستحقاق | عاجلة | تصعيد |
| **4** | بعد 10 أيام من الاستحقاق | إنذار نهائي | تحذير قانوني |

#### الخطوة 3: إرسال التذكيرات (يومياً الساعة 9 صباحاً)
```
يومياً (9:00 صباحاً)
    ↓
Cron Job: check_payment_reminders()
    ↓
البحث عن التذكيرات المعلقة:
- scheduled_date <= اليوم الحالي
- status = 'pending'
- الفاتورة لم تُدفع بعد
    ↓
إرسال التذكيرات عبر WhatsApp
```

### 🔧 المتطلبات التقنية

#### 1. إعداد WhatsApp Service (مرة واحدة فقط)

**أ) تثبيت المكتبات:**
```bash
npm install puppeteer-core @supabase/supabase-js dotenv
```

**ب) إعداد متغيرات البيئة:**
```env
SUPABASE_URL=your-supabase-url
SUPABASE_SERVICE_KEY=your-service-key
CHROME_EXECUTABLE_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe
WHATSAPP_SESSION_PATH=./whatsapp-session
```

**ج) ربط WhatsApp (مرة واحدة):**
```bash
node services/whatsapp-setup.js
# سيفتح متصفح Chrome مع WhatsApp Web
# امسح QR Code بهاتفك
# سيتم حفظ الجلسة تلقائياً
```

**د) تشغيل الخدمة:**
```bash
# الخدمة تعمل في الخلفية
node services/whatsapp-reminder-service.js

# أو باستخدام PM2 (للإنتاج):
pm2 start services/whatsapp-reminder-service.js --name whatsapp-reminders
```

### 📱 كيف يتم الإرسال؟

#### العملية الفنية:
```
1. Cron Job يتحقق من التذكيرات المعلقة
    ↓
2. العثور على التذكيرات المستحقة اليوم
    ↓
3. WhatsApp Service يقرأ التذكيرات من قاعدة البيانات
    ↓
4. لكل تذكيرة:
   - فتح WhatsApp Web
   - البحث عن رقم العميل
   - إرسال الرسالة
   - تحديث الحالة إلى 'sent'
   - تسجيل في reminder_history
    ↓
5. تحديث الإحصائيات
```

#### مثال على الرسالة:
```
المرحلة 1 (قبل 3 أيام):
السلام عليكم،
نود تذكيركم بأن فاتورة الإيجار رقم INV-2025-0123 
ستكون مستحقة يوم 1 من الشهر القادم.
المبلغ: 500 د.ك

المرحلة 2 (يوم الاستحقاق):
السلام عليكم،
نود تذكيركم بأن فاتورة الإيجار رقم INV-2025-0123 
مستحقة اليوم.
المبلغ: 500 د.ك
يرجى السداد في أقرب وقت ممكن.

المرحلة 3 (بعد 3 أيام):
السلام عليكم،
فاتورة الإيجار رقم INV-2025-0123 متأخرة 3 أيام.
المبلغ: 500 د.ك
يرجى السداد فوراً لتجنب الغرامات.

المرحلة 4 (بعد 10 أيام):
⚠️ إنذار نهائي
فاتورة الإيجار رقم INV-2025-0123 متأخرة 10 أيام.
المبلغ: 500 د.ك
في حالة عدم السداد خلال 48 ساعة:
- إيقاف الخدمات
- إجراءات قانونية
- الإبلاغ عن الديون
```

### 📊 مراقبة التذكيرات

**من واجهة المستخدم:**
```
1. Finance → Collections → WhatsApp Tab
2. أو Finance → Invoices → زر "التذكيرات" الأرجواني
3. عرض:
   - عدد الرسائل المرسلة اليوم
   - معدل النجاح
   - عدد الرسائل الفاشلة
   - حجم قائمة الانتظار
```

---

## 2️⃣ تحويل العملاء لقسم القضايا

### 🔄 العملية التلقائية

#### الخطوة 1: إضافة العملاء لقائمة المتأخرين
```
يوم 10 من كل شهر (تلقائياً)
    ↓
التحقق من العملاء الذين لم يدفعوا حتى يوم 10
    ↓
إضافة تلقائية إلى قائمة المتأخرين (Defaulters List)
```

#### الخطوة 2: مراقبة أيام التأخير
```
يومياً (9:00 صباحاً)
    ↓
Cron Job: process_overdue_invoices()
    ↓
حساب أيام التأخير لكل عميل
    ↓
تحديث حالة الفواتير إلى 'overdue'
```

#### الخطوة 3: مؤهلات الإجراء القانوني
```
العملاء المؤهلون تلقائياً:
- متأخرون 30 يوماً أو أكثر
- إجمالي المبلغ المستحق > 0
- الفاتورة لم تُدفع
```

### 📍 الوصول لقائمة المتأخرين

```
Legal → Defaulters List
أو
Finance → Collections → Defaulters Tab
```

### 🎯 عملية إنشاء القضايا القانونية

#### الطريقة 1: التلقائية (من قائمة المتأخرين)

```
1. افتح صفحة "قائمة المتأخرين"
    ↓
2. سيظهر لك قائمة بجميع العملاء المتأخرين:
   - اسم العميل
   - رقم العقد
   - أيام التأخير
   - المبلغ المستحق
   - عدد الأشهر غير المدفوعة
    ↓
3. حدد العملاء الذين تريد إنشاء قضايا لهم:
   - ☑️ ضع علامة بجانب كل عميل
   - أو اضغط "تحديد الكل"
    ↓
4. اضغط زر "إنشاء قضايا قانونية"
    ↓
5. النظام يقوم تلقائياً بـ:
   - إنشاء قضية قانونية لكل عميل
   - ملء جميع البيانات تلقائياً:
     * رقم القضية
     * عنوان القضية
     * نوع القضية: 'rental'
     * حالة القضية: 'active'
     * الأولوية: 'high' إذا تأخر ≥60 يوم، 'medium' إذا ≥30 يوم
     * معلومات العميل (الاسم، الهاتف، البريد)
     * قيمة القضية = المبلغ المستحق
     * الوصف التلقائي
     * التصنيفات: ['تأخر دفع', 'إيجار', 'تلقائي']
    ↓
6. تظهر رسالة نجاح: "تم إنشاء X قضية قانونية تلقائياً"
```

#### البيانات التلقائية المنشأة:

```typescript
{
  case_number: "CASE-2025-00123",  // رقم تلقائي
  case_title: "تأخر دفع الإيجار - [اسم العميل]",
  case_type: "rental",
  case_status: "active",
  priority: "high" | "medium",
  client_id: "[customer_id]",
  client_name: "[اسم العميل]",
  client_phone: "[رقم الهاتف]",
  description: "العميل متأخر عن دفع الإيجار لمدة X يوم. إجمالي المبلغ المستحق: X ريال",
  case_value: [المبلغ المستحق],
  tags: ["تأخر دفع", "إيجار", "تلقائي"],
  created_by: [user_id]
}
```

### 📋 قائمة المتأخرين - المعلومات المعروضة

| العمود | الوصف |
|--------|-------|
| **اسم العميل** | الاسم الكامل |
| **رقم العقد** | رقم العقد المرتبط |
| **المركبة** | رقم اللوحة |
| **أيام التأخير** | عدد الأيام منذ تاريخ الاستحقاق |
| **الأشهر غير المدفوعة** | عدد الأشهر المتأخرة |
| **المبلغ المستحق** | إجمالي المبلغ المستحق |
| **الإيجار الشهري** | قيمة الإيجار الشهري |
| **الغرامات** | إجمالي الغرامات المتراكمة |
| **آخر دفعة** | تاريخ آخر دفعة تم تسجيلها |
| **التواصل** | رقم الهاتف والبريد الإلكتروني |

### 🔔 ملاحظات مهمة

1. **التحديد التلقائي:** يتم إضافة العملاء تلقائياً يوم 10 من كل شهر
2. **المؤهلات:** فقط العملاء المتأخرون 30+ يوم مؤهلون للإجراء القانوني
3. **الإنشاء التلقائي:** يمكن إنشاء قضايا متعددة دفعة واحدة
4. **التكامل:** القضايا المنشأة مرتبطة بالعميل والفواتير تلقائياً

---

## 3️⃣ تسجيل الدفعات عند قدوم العملاء

### 🎯 السيناريوهات المختلفة

#### السيناريو 1: دفع فاتورة محددة

```
1. افتح صفحة "الفواتير" (Finance → Invoices)
    ↓
2. ابحث عن فاتورة العميل
    ↓
3. اضغط على الفاتورة
    ↓
4. اضغط زر "دفع الفاتورة" أو "Pay Invoice"
    ↓
5. ستفتح نافذة الدفع:
   - المبلغ: يظهر المبلغ المتبقي تلقائياً
   - طريقة الدفع: نقدي / تحويل / شيك
   - تاريخ الدفع: اليوم (يمكن تعديله)
   - رقم المرجع: اختياري
   - ملاحظات: اختياري
    ↓
6. اختر:
   - "دفع كامل" - يدفع المبلغ كاملاً
   - "دفع جزئي" - أدخل المبلغ المطلوب
    ↓
7. اضغط "تسجيل الدفعة"
    ↓
8. النظام يقوم تلقائياً بـ:
   - إنشاء سجل دفع في جدول payments
   - تحديث حالة الفاتورة:
     * إذا دفع كامل → status = 'paid'
     * إذا دفع جزئي → status = 'partial'
   - تحديث balance_due في الفاتورة
   - إنشاء قيد محاسبي (إذا كان مفعلاً)
   - إلغاء التذكيرات المعلقة لهذه الفاتورة
    ↓
9. رسالة نجاح: "تم تسجيل الدفعة بنجاح"
```

#### السيناريو 2: دفع إيجار شهري (من صفحة المتابعة المالية)

```
1. افتح صفحة "المتابعة المالية" (Finance → Financial Tracking)
    ↓
2. اختر العميل من القائمة
    ↓
3. سيظهر لك:
   - الشهر الحالي
   - المبلغ المستحق
   - الغرامة (إذا تأخر)
   - المبلغ المدفوع سابقاً
    ↓
4. اضغط "تسجيل دفعة"
    ↓
5. أدخل المعلومات:
   - تاريخ الدفع
   - المبلغ المدفوع
   - طريقة الدفع (نقدي / تحويل / شيك)
   - رقم المرجع (إذا كان تحويل)
   - المركبة (اختياري)
   - ملاحظات
    ↓
6. النظام يقوم تلقائياً بـ:
   - حساب الإيجار والغرامة تلقائياً بناءً على تاريخ الدفع
   - إنشاء إيصال دفع في rental_payment_receipts
   - إنشاء فاتورة تلقائياً (إذا كان هناك عقد)
   - تطبيق أي مبلغ زائد على غرامات الأشهر السابقة
   - تحديث حالة الدفع:
     * 'paid' إذا دفع كامل
     * 'partial' إذا دفع جزئي
     * 'pending' إذا لم يدفع
    ↓
7. رسالة نجاح: "تم تسجيل الدفعة بنجاح"
```

#### السيناريو 3: إنشاء إيصال دفع عام

```
1. Finance → Payments → "إيصال قبض جديد"
    ↓
2. اختر العميل من القائمة
    ↓
3. أدخل المعلومات:
   - رقم الإيصال
   - تاريخ الدفع
   - المبلغ
   - طريقة الدفع
   - العقد (اختياري)
   - الفاتورة (اختياري)
   - مركز التكلفة (اختياري)
   - البنك (اختياري)
   - الحساب المحاسبي (اختياري)
   - ملاحظات
    ↓
4. اضغط "حفظ"
    ↓
5. النظام يقوم تلقائياً بـ:
   - إنشاء سجل دفع في جدول payments
   - ربط الدفعة بالعميل (إذا كان موجوداً)
   - ربط الدفعة بالفاتورة (إذا كانت موجودة)
   - تحديث حالة الفاتورة تلقائياً
   - إنشاء قيد محاسبي (إذا كان مفعلاً)
    ↓
6. رسالة نجاح: "تم إنشاء إيصال القبض بنجاح"
```

### 💰 حساب الغرامات التلقائي

```
عند تسجيل دفعة:
    ↓
النظام يحسب تلقائياً:
    ↓
1. عدد الأيام المتأخرة:
   = تاريخ الدفع - تاريخ الاستحقاق (يوم 1)
    ↓
2. قيمة الغرامة:
   = عدد الأيام × 120 ريال/يوم
   الحد الأقصى: 3000 ريال/شهر
    ↓
3. المبلغ الإجمالي المستحق:
   = الإيجار الشهري + الغرامة
    ↓
4. المبلغ المتبقي:
   = المبلغ الإجمالي - المبلغ المدفوع
    ↓
5. حالة الدفع:
   - إذا المتبقي = 0 → 'paid'
   - إذا المتبقي > 0 → 'partial'
   - إذا لم يدفع → 'pending'
```

### 🔄 تطبيق المبالغ الزائدة على الغرامات السابقة

```
إذا دفع العميل أكثر من المبلغ المستحق:
    ↓
النظام يتحقق تلقائياً من:
    ↓
الأشهر السابقة التي لديها غرامات غير مدفوعة
    ↓
يطبق المبلغ الزائد على غرامة أقدم شهر غير مدفوع
    ↓
يحدث سجل الدفع السابق:
- pending_balance = المتبقي بعد دفع الغرامة
- payment_status = 'paid' أو 'partial'
- notes = "تم دفع غرامة التأخير من شهر X"
    ↓
إضافة ملاحظة تلقائية للدفعة الحالية:
"تم تطبيق X ريال لسداد غرامة شهر Y"
```

### 📝 مثال عملي: تسجيل دفعة

```
العميل: أحمد محمد
العقد: CNT-2024-001
الشهر: يناير 2025
تاريخ الاستحقاق: 1 يناير 2025
تاريخ الدفع الفعلي: 5 يناير 2025

الحساب التلقائي:
- الإيجار الشهري: 500 د.ك
- أيام التأخير: 4 أيام (5 - 1)
- الغرامة: 4 × 120 = 480 ريال = 0.480 د.ك
- المبلغ الإجمالي: 500 + 0.480 = 500.480 د.ك

إذا دفع العميل 600 د.ك:
- المبلغ المتبقي لهذا الشهر: 0 (دفع كامل)
- المبلغ الزائد: 600 - 500.480 = 99.520 د.ك
- النظام يبحث عن غرامات سابقة غير مدفوعة
- إذا وجد غرامة شهر ديسمبر 2024 = 0.400 د.ك
- يطبق 0.400 د.ك على غرامة ديسمبر
- المتبقي: 99.520 - 0.400 = 99.120 د.ك (يمكن تطبيقه على دفعات أخرى)
```

### 📊 بعد تسجيل الدفعة

#### ما يحدث تلقائياً:

1. **تحديث الفاتورة:**
   - `payment_status` → 'paid' أو 'partial'
   - `balance_due` → يتم تحديثه
   - `amount_paid` → يتم تحديثه

2. **إلغاء التذكيرات:**
   - جميع التذكيرات المعلقة لهذه الفاتورة → `status = 'cancelled'`

3. **إنشاء القيد المحاسبي:**
   - إذا كان `autoCreateJournalEntry = true`
   - يتم إنشاء قيد تلقائي في `journal_entries`

4. **تحديث إحصائيات:**
   - يتم تحديث لوحة الإحصائيات فوراً
   - يتم تحديث المتابعة المالية

### 🎨 واجهة المستخدم

#### صفحة المتابعة المالية:
```
┌─────────────────────────────────────────┐
│  المتابعة المالية                        │
├─────────────────────────────────────────┤
│  العميل: [اختر من القائمة]              │
│  السنة: [2025]    الشهر: [يناير]       │
├─────────────────────────────────────────┤
│  المبلغ المستحق: 500.480 د.ك            │
│  الإيجار: 500 د.ك                        │
│  الغرامة: 0.480 د.ك (4 أيام)            │
│  المبلغ المدفوع: 0 د.ك                   │
│  المبلغ المتبقي: 500.480 د.ك            │
├─────────────────────────────────────────┤
│  [تسجيل دفعة]                           │
└─────────────────────────────────────────┘
```

#### نموذج تسجيل الدفعة:
```
┌─────────────────────────────────────────┐
│  تسجيل دفعة                             │
├─────────────────────────────────────────┤
│  تاريخ الدفع: [05/01/2025]              │
│  المبلغ المدفوع: [600.000]             │
│  طريقة الدفع: [نقدي ▼]                 │
│  المركبة: [ABC-123 ▼]                   │
│  رقم المرجع: [اختياري]                │
│  ملاحظات: [اختياري]                     │
├─────────────────────────────────────────┤
│  ملخص:                                  │
│  - الإيجار: 500.000 د.ك                 │
│  - الغرامة: 0.480 د.ك                   │
│  - الإجمالي: 500.480 د.ك                │
│  - المدفوع: 600.000 د.ك                  │
│  - المتبقي: 0 د.ك                        │
│  - المبلغ الزائد: 99.520 د.ك            │
│    (سيتم تطبيقه على غرامات سابقة)       │
├─────────────────────────────────────────┤
│  [حفظ]  [إلغاء]                         │
└─────────────────────────────────────────┘
```

---

## 🔄 الدورة الكاملة: من الفاتورة إلى الدفع

```
يوم 28 من الشهر
    ↓
إنشاء فواتير للشهر القادم
    ↓
جدولة 4 تذكيرات تلقائياً
    ↓
يوم 1 من الشهر (تاريخ الاستحقاق)
    ↓
إرسال تذكيرة يوم الاستحقاق (المرحلة 2)
    ↓
يوم 4 (بعد 3 أيام)
    ↓
إرسال تذكيرة تأخير (المرحلة 3)
    ↓
يوم 10
    ↓
إضافة العميل لقائمة المتأخرين (إذا لم يدفع)
    ↓
يوم 11 (بعد 10 أيام)
    ↓
إرسال إنذار نهائي (المرحلة 4)
    ↓
يوم 30+ (بعد 30 يوماً)
    ↓
العميل مؤهل للإجراء القانوني
    ↓
يمكن إنشاء قضية قانونية تلقائياً
    ↓
[العميل يدفع]
    ↓
تسجيل الدفعة في النظام
    ↓
تحديث حالة الفاتورة تلقائياً
    ↓
إلغاء جميع التذكيرات المعلقة
    ↓
حساب الغرامات تلقائياً
    ↓
إنشاء قيد محاسبي (إذا كان مفعلاً)
    ↓
انتهاء العملية ✅
```

---

## 📋 ملخص سريع

### إرسال الواتساب:
- ✅ تلقائي 100%
- ✅ 4 مراحل (قبل 3 أيام، يوم الاستحقاق، بعد 3 أيام، بعد 10 أيام)
- ✅ يحتاج إعداد WhatsApp Service (مرة واحدة)

### تحويل للقضايا:
- ✅ إضافة تلقائية يوم 10 من كل شهر
- ✅ مؤهل بعد 30 يوم تأخير
- ✅ إنشاء قضايا تلقائياً من واجهة المستخدم

### تسجيل الدفعات:
- ✅ من صفحة الفواتير
- ✅ من صفحة المتابعة المالية
- ✅ حساب تلقائي للغرامات
- ✅ تطبيق تلقائي على الغرامات السابقة

---

**الملف جاهز للاستخدام والمرجع!** 📚

