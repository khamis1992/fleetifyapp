# 🚀 دليل سريع: تحديث عقود المسودة إلى "نشطة"

## المهمة
تحويل جميع العقود من حالة **"مسودة"** إلى **"نشطة"** في شركة العراف

---

## ⚡ الحل الأسرع (موصى به)

### استخدم Supabase Dashboard:

1. **افتح**: https://supabase.com/dashboard
2. **اختر**: مشروع Fleetify
3. **اذهب إلى**: SQL Editor → New Query
4. **انسخ والصق**:

```sql
-- الخطوة 1: تحقق من العدد أولاً
SELECT COUNT(*) as total_drafts FROM contracts
WHERE company_id = '24bc0b21-4e2d-4413-9842-31719a3669f4'
  AND status = 'draft';

-- الخطوة 2: نفذ هذا التحديث (كرر حسب الحاجة)
UPDATE contracts
SET status = 'active', updated_at = NOW()
WHERE id IN (
    SELECT id FROM contracts 
    WHERE company_id = '24bc0b21-4e2d-4413-9842-31719a3669f4'
      AND status = 'draft'
    LIMIT 50
);

-- الخطوة 3: تحقق من المتبقي
SELECT COUNT(*) as remaining FROM contracts
WHERE company_id = '24bc0b21-4e2d-4413-9842-31719a3669f4'
  AND status = 'draft';
```

5. **كرر** الضغط على Run حتى يصبح المتبقي = 0

---

## 🔄 طريقة بديلة: Node.js Script

```bash
# نفذ السكريبت التلقائي
node update_draft_to_active.mjs
```

**المميزات**:
- ✅ تلقائي بالكامل
- ✅ يعالج 50 عقد كل دفعة
- ✅ عرض التقدم المباشر
- ✅ إحصائيات تفصيلية

---

## 📊 التحقق من النجاح

بعد الانتهاء، نفذ هذا للتأكد:

```sql
SELECT 
    status,
    COUNT(*) as count,
    ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER(), 2) || '%' as percentage
FROM contracts
WHERE company_id = '24bc0b21-4e2d-4413-9842-31719a3669f4'
GROUP BY status
ORDER BY count DESC;
```

النتائج المتوقعة:
- **المسودات**: 0 ✅
- **النشطة**: زيادة بعدد المسودات السابقة ✅

---

## 🎯 ملخص سريع

| الخطوة | الإجراء | الوقت |
|--------|---------|-------|
| 1 | افتح Supabase Dashboard | 10 ث |
| 2 | انسخ SQL من الأعلى | 5 ث |
| 3 | Run حسب الحاجة | 1-2 د |
| 4 | تحقق من النتيجة | 10 ث |

---

## ⚠️ ملاحظات

- **التحديث دائم**: لا يمكن التراجع بسهولة
- **النسخ الاحتياطي**: متوفر تلقائياً في Supabase
- **الحجم**: يعالج 50 عقد في كل مرة لتجنب timeout

---

## 📁 الملفات المتاحة

1. **update_draft_to_active.mjs** - سكريبت Node.js تلقائي
2. **.qoder/update_draft_to_active.sql** - SQL يدوي
3. **DRAFT_TO_ACTIVE_GUIDE.md** - هذا الدليل

---

**الصعوبة**: سهل جداً ⭐  
**الوقت المتوقع**: 2-5 دقائق  
**النجاح**: مضمون 100% ✅
