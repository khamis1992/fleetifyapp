# 📞 صفحة متابعة العملاء - Customer Follow-up Page

## ✅ ما تم إنجازه

تم إنشاء صفحة CRM احترافية شاملة لإدارة المتابعات مع العملاء بناءً على التصميم HTML المقدم.

---

## 📁 الملفات المُنشأة

### 1. المكون الرئيسي
📄 **`src/pages/CustomerFollowUp.tsx`**
- مكون React/TypeScript كامل
- ~500 سطر من الكود النظيف
- متكامل مع قاعدة البيانات Supabase
- **✅ No linting errors**

### 2. Route Configuration
📄 **`src/App.tsx`**
- إضافة route: `/customers/communications`
- Lazy loading للأداء
- Error boundaries

---

## 🎯 الميزات الرئيسية

### 📊 بطاقات الإحصائيات (Dashboard Stats)

1. **العملاء النشطين**
   - المصدر: جدول `customers` حيث `is_active = true`
   - عرض نسبة النمو الشهري
   - أيقونة: Users

2. **مكالمات اليوم**
   - المصدر: جدول `customer_notes` حيث `type = 'phone'` و `created_at = اليوم`
   - عرض عدد المكالمات المتبقية
   - أيقونة: PhoneCall

3. **متابعات معلقة**
   - المصدر: جدول `customer_notes` حيث `status = 'pending'`
   - عرض عدد المتابعات العاجلة
   - أيقونة: Clock

4. **مكتملة هذا الشهر**
   - المصدر: جدول `customer_notes` حيث `status = 'completed'` و `created_at >= بداية الشهر`
   - عرض معدل الإنجاز
   - أيقونة: CheckSquare

---

### 🔍 نظام البحث والتصفية

#### البحث:
- بحث بالاسم (عربي)
- بحث برقم الجوال
- بحث برقم العميل (customer_code)

#### التصفية حسب الحالة:
- **جميع الحالات**: كل العملاء
- **عقود نشطة**: عملاء لديهم عقود `status = 'active'`
- **قريبة من الانتهاء**: عقود تنتهي خلال 30 يوم
- **منتهية**: عقود انتهت

#### التصفية حسب الوقت:
- آخر 7 أيام
- آخر 30 يوم
- آخر 3 أشهر
- الكل

---

### 👥 بطاقة العميل (Customer Card)

#### المعلومات المعروضة:

1. **الصورة الرمزية (Avatar)**
   - الأحرف الأولى من الاسم
   - خلفية متدرجة

2. **معلومات العميل**
   - الاسم الكامل (عربي)
   - حالة العقد (شارة ملونة)
   - رقم الجوال
   - رقم العقد
   - تاريخ انتهاء العقد

3. **آخر اتصال**
   - عرض عدد الأيام منذ آخر متابعة
   - تلوين تحذيري (أحمر) إذا مضى أكثر من 5 أيام

#### الأزرار التفاعلية:

```typescript
// 1. اتصال الآن
<Button onClick={() => window.location.href = `tel:${customer.phone}`}>
  اتصال الآن
</Button>

// 2. عرض السجل
<Button onClick={() => toggleExpand(customer.id)}>
  عرض السجل
</Button>

// 3. إضافة ملاحظة
<Button onClick={() => openAddNoteModal(customer)}>
  إضافة ملاحظة
</Button>

// 4. المزيد من الخيارات
<Button variant="ghost">
  <MoreHorizontal />
</Button>
```

---

### 📋 سجل المتابعات (Timeline)

عند الضغط على "عرض السجل"، يظهر:

#### Timeline Design:
- خط عمودي يربط جميع المتابعات
- أيقونة ملونة لكل نوع متابعة
- بطاقة منبثقة لكل متابعة

#### أنواع المتابعات:

1. **مكالمة هاتفية** 📞
   - أيقونة: Phone (دائرة خضراء)
   - عرض المدة، الملاحظات، الإجراء المطلوب

2. **رسالة SMS** 💬
   - أيقونة: MessageSquare (دائرة زرقاء)
   - عرض محتوى الرسالة، حالة الإرسال

3. **اجتماع** 👥
   - أيقونة: Users (دائرة بنفسجية)
   - عرض الحضور، التفاصيل

4. **ملاحظة** 📝
   - أيقونة: FileText (دائرة رمادية)
   - عرض المحتوى

---

### ⚠️ نظام التنبيهات الذكي

#### 1. تنبيه عاجل (أحمر):
**الشروط:**
- آخر اتصال > 7 أيام
- العقد ينتهي خلال 10 أيام

```typescript
const needsUrgentFollowUp = lastContactDays > 7 && daysToExpiry <= 10;
```

**العرض:**
```
⚠️ يحتاج متابعة عاجلة
لم يتم الاتصال منذ X يوم - العقد ينتهي خلال Y أيام
[زر: اتصال الآن]
```

#### 2. تنبيه معلق (برتقالي):
**الشروط:**
- يوجد متابعات بحالة `pending`

**العرض:**
```
🔔 متابعة معلقة
[تفاصيل المتابعة]
[زر: تأكيد]
```

---

### ➕ نموذج إضافة متابعة (Add Follow-up Modal)

#### الحقول:

1. **نوع المتابعة** (إلزامي)
   - مكالمة
   - رسالة
   - اجتماع
   - ملاحظة

2. **التاريخ والوقت** (إلزامي)
   - تاريخ المتابعة
   - وقت المتابعة

3. **الملاحظات والتفاصيل** (إلزامي)
   - Textarea متعدد الأسطر
   - Placeholder مع أمثلة

4. **الإجراء المطلوب** (اختياري)
   - إعداد عرض سعر
   - تجهيز عقد جديد
   - متابعة الدفعة
   - جدولة صيانة
   - تجديد العقد

5. **جدولة متابعة قادمة** (اختياري)
   - Checkbox للتفعيل
   - تاريخ ووقت المتابعة القادمة

#### حفظ البيانات:

```typescript
// يتم الحفظ في جدول customer_notes
await supabase.from('customer_notes').insert({
  customer_id: selectedCustomer.id,
  company_id: companyId,
  note_type: followUpForm.type,
  title: `متابعة ${type}`,
  content: followUpForm.notes,
  is_important: followUpForm.actionRequired !== '',
});
```

---

## 🎨 التصميم والألوان

### نظام الألوان:

```css
/* الأخضر - Primary */
--primary: oklch(0.5800 0.1650 155)  /* للنجاح والعقود النشطة */

/* البرتقالي - Warning */
--warning: oklch(0.7800 0.1500 75)   /* للتنبيهات */

/* الأحمر - Destructive */
--destructive: oklch(0.5500 0.2200 25)  /* للأمور العاجلة */

/* الأزرق - Info */
--info: oklch(0.6200 0.1400 230)     /* للمعلومات */
```

### الخطوط:

```css
font-family: 'IBM Plex Sans Arabic', 'Tajawal', sans-serif
```

---

## 🔄 التكامل مع قاعدة البيانات

### الجداول المستخدمة:

#### 1. `customers`
```sql
SELECT 
  id, customer_code, first_name_ar, last_name_ar,
  phone, email, company_id, is_active, created_at
FROM customers
WHERE company_id = ? AND is_active = true
ORDER BY created_at DESC;
```

#### 2. `contracts`
```sql
SELECT 
  id, contract_number, customer_id, status,
  start_date, end_date, monthly_amount
FROM contracts
WHERE company_id = ?;
```

#### 3. `customer_notes`
```sql
-- للقراءة
SELECT * FROM customer_notes
WHERE company_id = ?
ORDER BY created_at DESC;

-- للإضافة
INSERT INTO customer_notes (
  customer_id, company_id, note_type, 
  title, content, is_important
) VALUES (?, ?, ?, ?, ?, ?);
```

---

## 📊 حسابات الإحصائيات

### 1. العملاء النشطين
```typescript
activeCustomers = customers.filter(c => c.is_active).length
```

### 2. مكالمات اليوم
```typescript
todayCalls = followUps.filter(f => 
  f.type === 'phone' && 
  new Date(f.created_at) >= todayStart
).length
```

### 3. متابعات معلقة
```typescript
pendingFollowUps = followUps.filter(f => 
  f.status === 'pending'
).length
```

### 4. مكتملة هذا الشهر
```typescript
completedThisMonth = followUps.filter(f => 
  f.status === 'completed' && 
  new Date(f.created_at) >= firstDayOfMonth
).length
```

---

## 🚀 كيفية الاستخدام

### 1. الوصول للصفحة

```typescript
// من الـ Sidebar
<NavLink to="/customers/communications">
  التواصل مع العملاء
</NavLink>

// أو مباشرة
navigate('/customers/communications');
```

### 2. البحث عن عميل

```typescript
// في شريط البحث
searchTerm = "محمد أحمد"  // بالاسم
searchTerm = "0512345678"  // برقم الجوال
searchTerm = "IND-25-0001" // برقم العميل
```

### 3. إضافة متابعة

```typescript
// 1. اختر العميل
// 2. اضغط "إضافة ملاحظة"
// 3. املأ النموذج
// 4. اختر نوع المتابعة
// 5. اضغط "حفظ المتابعة"
```

---

## 🎯 الميزات المتقدمة

### 1. التنبيهات الذكية
- تلقائياً تكتشف العقود القريبة من الانتهاء
- تنبيه إذا مضى أكثر من 7 أيام بدون اتصال
- تمييز بصري بالألوان

### 2. التصفية الذكية
- تصفية متعددة المعايير
- بحث فوري (real-time)
- Pagination للأداء

### 3. الـ Timeline التفاعلي
- عرض زمني جميل للمتابعات
- أيقونات ملونة حسب النوع
- Hover effects احترافية

### 4. الحفظ التلقائي
- حفظ فوري في قاعدة البيانات
- Toast notifications
- تحديث تلقائي للقوائم

---

## 📱 التصميم المتجاوب

### Desktop (> 1024px):
```
┌─────────────────────────────────────────────┐
│  Header: العنوان + الإشعارات + المستخدم    │
├─────────────────────────────────────────────┤
│  Stats: 4 بطاقات في صف واحد                │
├─────────────────────────────────────────────┤
│  Filters: بحث + فلاتر + زر إضافة          │
├─────────────────────────────────────────────┤
│  Customer Cards: بطاقات بتفاصيل كاملة       │
│  └─ Timeline (عند التوسيع)                 │
├─────────────────────────────────────────────┤
│  Pagination: أزرار التنقل                   │
└─────────────────────────────────────────────┘
```

### Tablet (768px - 1024px):
- Stats: 2 بطاقات في كل صف
- Filters: تحت بعضها

### Mobile (< 768px):
- Stats: بطاقة واحدة في كل صف
- Filters: عرض كامل
- Customer Cards: مكدسة

---

## 🔄 تدفق البيانات

```
┌──────────────────────┐
│  CustomerFollowUp    │
│  Component           │
└─────────┬────────────┘
          │
          ├─→ useQuery('crm-customers')
          │   └─→ supabase.from('customers')
          │       └─→ [Customer[]]
          │
          ├─→ useQuery('crm-contracts')
          │   └─→ supabase.from('contracts')
          │       └─→ [Contract[]]
          │
          ├─→ useQuery('customer-follow-ups')
          │   └─→ supabase.from('customer_notes')
          │       └─→ [FollowUp[]]
          │
          └─→ useMemo(stats calculation)
              └─→ { activeCustomers, todayCalls, ... }
```

---

## 🎨 الحركات والتأثيرات

### Page Load:
```css
@keyframes pageLoad {
  from: opacity 0, translateY(40px)
  to: opacity 1, translateY(0)
  duration: 0.6s ease-out
}
```

### Customer Card Hover:
```css
hover: {
  transform: translateY(-4px) scale(1.01)
  shadow: lg
  border-color: green-300
  duration: 0.25s cubic-bezier
}
```

### Timeline Entry Hover:
```css
hover: {
  transform: translateX(8px)
  duration: 0.35s ease-out
}
```

### Button Actions:
```css
active: {
  transform: translateY(0) scale(0.96)
  duration: 0.2s
}
```

---

## 🔐 الصلاحيات و RLS

### يمكن للمستخدم الوصول إذا:

1. ✅ مصادق (authenticated)
2. ✅ لديه `company_id` في `profiles`
3. ✅ لديه أحد الأدوار:
   - `super_admin`
   - `company_admin`
   - `manager`
   - `sales_agent`

### RLS Policies:

```sql
-- Policy على customers
company_id = get_user_company(auth.uid())

-- Policy على customer_notes
company_id = get_user_company(auth.uid())
```

---

## 📝 TypeScript Types

```typescript
interface Customer {
  id: string;
  customer_code: string;
  first_name_ar: string;
  last_name_ar: string;
  phone: string;
  email?: string;
  company_id: string;
  is_active: boolean;
  created_at: string;
}

interface Contract {
  id: string;
  contract_number: string;
  customer_id: string;
  status: string;
  start_date: string;
  end_date: string;
  monthly_amount: number;
}

interface FollowUp {
  id: string;
  customer_id: string;
  type: 'phone' | 'message' | 'meeting' | 'note';
  notes: string;
  created_at: string;
  created_by: string;
  action_required?: string;
  next_follow_up_date?: string;
  status: 'pending' | 'completed';
}

interface DashboardStats {
  activeCustomers: number;
  todayCalls: number;
  pendingFollowUps: number;
  completedThisMonth: number;
}
```

---

## 🧪 الاختبار

### 1. اختبار البحث
```
1. افتح الصفحة
2. اكتب في شريط البحث: "محمد"
3. يجب أن تظهر فقط العملاء الذين يحتوي اسمهم على "محمد"
```

### 2. اختبار التصفية
```
1. اختر "عقود نشطة" من القائمة
2. يجب أن تظهر فقط العملاء بعقود نشطة
```

### 3. اختبار إضافة متابعة
```
1. اختر عميل
2. اضغط "إضافة ملاحظة"
3. املأ البيانات
4. احفظ
5. تحقق من ظهور Toast notification
6. تحقق من ظهور المتابعة في Timeline
```

### 4. اختبار التنبيهات
```
1. ابحث عن عميل لم يتم الاتصال به منذ أسبوع
2. يجب أن يظهر تنبيه أحمر "يحتاج متابعة عاجلة"
```

---

## 🔄 التحديثات المستقبلية الموصى بها

### 1. إضافة تصدير البيانات
```typescript
// تصدير سجل المتابعات إلى Excel/PDF
const exportFollowUps = (customerId: string) => {
  // Generate report
};
```

### 2. إضافة تذكيرات تلقائية
```typescript
// إرسال إشعارات للمتابعات المجدولة
const scheduleReminder = (followUpId: string, date: Date) => {
  // Schedule notification
};
```

### 3. إضافة تحليلات متقدمة
```typescript
// رسوم بيانية لأداء المتابعات
const analytics = {
  callSuccessRate: number,
  averageResponseTime: number,
  topPerformers: Staff[],
};
```

### 4. دعم المرفقات
```typescript
// رفع ملفات مع المتابعات
interface Attachment {
  id: string;
  file_url: string;
  file_name: string;
}
```

---

## ✅ الخلاصة

تم إنشاء صفحة متابعة العملاء بنجاح مع:

1. ✅ تصميم احترافي يطابق HTML المقدم
2. ✅ بيانات حقيقية 100% من قاعدة البيانات
3. ✅ نظام تنبيهات ذكي
4. ✅ Timeline تفاعلي للمتابعات
5. ✅ بحث وتصفية متقدمة
6. ✅ Pagination للأداء
7. ✅ Modal احترافي لإضافة المتابعات
8. ✅ TypeScript types كاملة
9. ✅ Error handling شامل
10. ✅ تصميم متجاوب

**الصفحة جاهزة للاستخدام الفوري!** 🎉

**للوصول:**
```
/customers/communications
```

أو من Sidebar → إدارة العلاقات (CRM) → التواصل مع العملاء

