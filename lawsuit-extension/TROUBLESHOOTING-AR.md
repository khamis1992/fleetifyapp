# 🔧 استكشاف أخطاء إضافة رفع الدعاوى

## المشكلة: لا يتم ملء البيانات تلقائياً

### 📊 التشخيص

#### أولاً: التحقق من Console

1. **افتح Console في Chrome:**
   - اضغط `F12`
   - اختر تبويب `Console`

2. **ابحث عن الأخطاء:**
   - ابحث عن أي رسائل خطأ باللون الأحمر
   - ابحث عن رسائل `❌` من الإضافة

#### ثانياً: التحقق من الإضافة

1. **افتح صفحة الإضافة:**
   - اذهب إلى `chrome://extensions/`
   - ابحث عن "نظام رفع الدعاوى التلقائي - العراف"
   - تأكد من أنها مفعّلة

2. **تفقد الأذونات:**
   - اضغط "تفاصيل" أو "Permissions"
   - تأكد من وجود:
     - ✅ storage
     - ✅ activeTab
     - ✅ tabs
     - ✅ scripting

3. **عرض السجلات:**
   - اضغط "inspect views: background page"
   - افتح Console
   - ابحث عن أخطاء

---

## 🔧 حلول المشاكل الشائعة

### المشكلة 1: "لا توجد بيانات محفوظة"

**السبب:** البيانات لم تُحفظ من صفحة العراف

**الحل:**
1. افتح صفحة تجهيز دعوى في العراف
2. تأكد من اكتمال البيانات:
   - ✅ بيانات المدعى عليه
   - ✅ ملخص المطالبة
   - ✅ بيانات تقاضي (العنوان، الوقائع، الطلبات)
3. اضغط "إرسال لتقاضي (الإضافة)"
4. ابحث عن رسالة: "✅ تم حفظ البيانات"

---

### المشكلة 2: الإضافة لا تفتح تقاضي

**السبب:** إذن `tabs` غير موجود في `manifest.json`

**الحل:**
1. تأكد من تحديث `manifest.json`:
   ```json
   "permissions": [
     "storage",
     "activeTab",
     "tabs",      // ← يجب أن يكون موجوداً
     "scripting"  // ← يجب أن يكون موجوداً
   ]
   ```

2. **إعادة تثبيت الإضافة:**
   - اذهب إلى `chrome://extensions/`
   - احذف الإضافة
   - أعد تثبيتها من مجلد `lawsuit-extension`

---

### المشكلة 3: تقاضي يفتح لكن لا يتم ملء البيانات

**السبب:** رسالة `autoFill` لا تُرسل إلى content script

**الحل:**
1. **افتح Console في تبويب تقاضي**
2. **ابحث عن الرسائل:**
   ```
   ✅ تم تحميل الإضافة المتقدمة لموقع تقاضي
   📩 رسالة واردة: autoFill
   ```
3. **إذا لم تجد "رسالة واردة: autoFill":**
   - المشكلة في `background.js`
   - تأكد من وجود معالج `case 'autoFill'`

---

### المشكلة 4: البيانات تُحفظ لكن لا تُسترجع

**السبب:** اسم المفتاح في storage غير صحيح

**الحل:**
1. افتح Console في أي صفحة
2. نفذ:
   ```javascript
   chrome.storage.local.get(['lawsuitData'], (result) => {
     console.log('البيانات المحفوظة:', result);
   });
   ```
3. إذا كان `result.lawsuitData` فارغ:
   - المشكلة في حفظ البيانات من صفحة العراف
   - تحقق من Console في صفحة العراف

---

### المشكلة 5: زر "ملء البيانات" لا يظهر في تقاضي

**السبب:** Content script لم يُحمّل

**الحل:**
1. أعد تحميل صفحة تقاضي (`F5`)
2. تحقق من Console:
   ```
   ✅ تم تحميل الإضافة المتقدمة لموقع تقاضي
   ✅ تم إضافة أزرار الإضافة
   ```
3. إذا لم تظهر الرسائل:
   - المشكلة في `manifest.json`
   - تحقق من `content_scripts` قسم

---

## 📋 خطوات التشخيص الشاملة

### الخطوة 1: تحديث الإضافة

```bash
1. احذف الإضافة القديمة من chrome://extensions/
2. تأكد من أن ملفات الإضافة محدثة:
   - manifest.json (يحتوي على permissions: tabs, scripting)
   - background.js (يحتوي على معالج autoFill)
   - content-taqadi.js (النسخة المتقدمة)
3. أعد تثبيت الإضافة من مجلد lawsuit-extension
```

### الخطوة 2: اختبار من صفحة العراف

1. افتح صفحة تجهيز دعوى في العراف
2. افتح Console (`F12`)
3. ابحث عن:
   ```
   ✅ تم حفظ البيانات في تخزين الإضافة
   ```
4. إذا لم تظهر الرسالة:
   - البيانات لم تُحفظ
   - تحقق من `content-alaraf.js`

### الخطوة 3: اختبار على تقاضي

1. اضغط "رفع تلقائي (متصفحك المحلي)" في العراف
2. انتظر فتح تقاضي
3. افتح Console في تبويب تقاضي
4. ابحث عن:
   ```
   ✅ تم تحميل الإضافة المتقدمة لموقع تقاضي
   📩 رسالة واردة: autoFill
   🚀 بدء الأتمتة الكاملة...
   ```
5. إذا لم تظهر الرسائل:
   - الرسالة لم تُرسل من background
   - تحقق من `background.js`

---

## 🐛 جمع معلومات التشخيص

إذا استمرت المشكلة، اجمع المعلومات التالية:

### من Console في صفحة العراف:
```
[انسخ أي رسائل من الإضافة]
✅ تم تحميل إضافة رفع الدعاوى على alaraf.online
📩 استلام بيانات من صفحة العراف: {...}
✅ تم حفظ البيانات في تخزين الإضافة
```

### من Console في تقاضي:
```
[انسخ أي رسائل من الإضافة]
✅ تم تحميل الإضافة المتقدمة لموقع تقاضي
📩 رسالة واردة: autoFill
📋 البيانات المستلمة: {...}
🚀 بدء الأتمتة الكاملة...
```

### من background page:
```
1. اذهب إلى chrome://extensions/
2. اضغط "inspect views: background page"
3. افتح Console
4. انسخ جميع الرسائل
```

---

## 🔍 أدوات التشخيص

### 1. التحقق من البيانات المحفوظة

افتح Console في أي صفحة ونفذ:

```javascript
chrome.storage.local.get(['lawsuitData', 'savedAt'], (result) => {
  console.log('=== بيانات الإضافة ===');
  console.log('lawsuitData:', result.lawsuitData);
  console.log('savedAt:', result.savedAt);
  
  if (!result.lawsuitData) {
    console.error('❌ لا توجد بيانات محفوظة!');
  } else {
    console.log('✅ البيانات موجودة');
    console.log('المدعى عليه:', result.lawsuitData.defendant);
    console.log('العنوان:', result.lawsuitData.texts?.title);
  }
});
```

### 2. اختبار إرسال رسالة للإضافة

افتح Console في أي صفحة ونفذ:

```javascript
chrome.runtime.sendMessage({
  action: 'ping'
}, (response) => {
  console.log('استجابة الإضافة:', response);
  console.log('الحالة:', response.status);
});
```

النتيجة المتوقعة:
```
استجابة الإضافة: {status: 'alive'}
الحالة: alive
```

### 3. اختبار حفظ البيانات

افتح Console في أي صفحة ونفذ:

```javascript
const testData = {
  defendant: { name: 'اختبار', nationalId: '123', phone: '123456' },
  texts: { title: 'عنوان تجريبي', facts: 'وقائع تجريبية', claims: 'طلبات تجريبية' },
  extractedAt: new Date().toISOString()
};

chrome.runtime.sendMessage({
  action: 'saveLawsuitData',
  data: testData
}, (response) => {
  console.log('استجابة الحفظ:', response);
  
  if (response.success) {
    console.log('✅ تم الحفظ بنجاح');
  } else {
    console.error('❌ فشل الحفظ:', response.error);
  }
});
```

---

## 📞 عند طلب المساعدة

عند الإبلاغ عن مشكلة، اذكر:

1. **الخطوات التي قمت بها:**
   - ماذا ضغطت؟
   - ماذا ظهر في الشاشة؟

2. **رسائل Console:**
   - من صفحة العراف
   - من صفحة تقاضي
   - من background page

3. **الإصدارات:**
   - إصدار الإضافة (من manifest.json)
   - إصدار Chrome
   - نظام التشغيل

4. **النتيجة المتوقعة:**
   - ماذا كنت تتوقع أن يحدث؟
   - ماذا حدث فعلياً؟

---

## ✅ التحقق من الإصلاح

بعد تطبيق الحل، تأكد من:

- [ ] الإضافة مُحدثة إلى الإصدار 1.1.0 أو أحدث
- [ ] الأذونات: storage, activeTab, tabs, scripting موجودة
- [ ] Console في العراف يعرض "تم حفظ البيانات"
- [ ] Console في تقاضي يعرض "بدء الأتمتة"
- [ ] البيانات تظهر في chrome.storage.local
- [ ] زر "ملء البيانات" يظهر في تقاضي

---

**آخر تحديث:** 31 ديسمبر 2025

