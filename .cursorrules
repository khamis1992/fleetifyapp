# Cursor Rules - FleetifyApp

## 📊 قاعدة البيانات (Database)

**قبل أي عمل مع قاعدة البيانات:**
1. اقرأ `.claude/DATABASE_SCHEMA_REFERENCE.md` للحصول على بنية الجداول الصحيحة
2. تحقق من أسماء الأعمدة الحقيقية
3. لا تفترض وجود أعمدة بناءً على الاسم المنطقي

**القواعد الذهبية:**
- ❌ لا تخمن أسماء الأعمدة
- ✅ دائماً تحقق من `.claude/DATABASE_SCHEMA_REFERENCE.md`
- ✅ استخدم `src/integrations/supabase/types.ts` للتأكد

**أخطاء شائعة:**
```
❌ description → ✅ line_description (في journal_entry_lines)
❌ level → ✅ account_level (في chart_of_accounts)  
❌ parent_code → ✅ parent_account_code
❌ account_name_en → ✅ account_name
```

---

## 🏢 معلومات الشركة الرئيسية

**شركة العراف:**
- Company ID: `24bc0b21-4e2d-4413-9842-31719a3669f4`
- Currency: QAR
- Business Type: car_rental

---

## 💰 النظام المالي

### القيود المحاسبية
- **journal_entries** - القيد الرئيسي
- **journal_entry_lines** - سطور القيد (يجب أن يحتوي كل قيد على سطرين على الأقل)

### قواعد القيود:
1. كل قيد يجب أن يكون متوازن (total_debit = total_credit)
2. السطور يجب أن تحتوي على `line_number` (1, 2, 3...)
3. الحسابات المستخدمة يجب أن تكون:
   - `is_header = false`
   - `account_level >= 3`

---

## 🚗 الجداول التشغيلية

- **contracts** - العقود (588 عقد)
- **customers** - العملاء (781 عميل)
- **vehicles** - المركبات (510 مركبة)
- **invoices** - الفواتير (1,250 فاتورة)
- **payments** - المدفوعات (6,568 دفعة)

---

## 📖 للمزيد من التفاصيل

راجع: `.claude/DATABASE_SCHEMA_REFERENCE.md`

---

## ⚠️ قواعد توفير التوكنات (Token Optimization)

**ممنوع منعاً باتاً:**
- ❌ لا تنشئ أي ملفات توثيق تلقائياً (.md أو .txt) بعد كل عملية
- ❌ لا تنشئ تقارير أو ملخصات إلا إذا طلب المستخدم صراحةً
- ❌ لا تنشئ ملفات في مجلد `.cursor/` إلا للضرورة القصوى
- ❌ لا تكتب ملخصات طويلة أو شروحات مفصلة بعد كل تعديل
- ❌ لا تنشئ ملفات README أو GUIDE تلقائياً
- ❌ لا تنشئ ملفات STATUS أو REPORT أو SUMMARY تلقائياً

**القواعد الواجبة:**
- ✅ أجب على السؤال مباشرة بدون كلام زائد
- ✅ نفذ المطلوب فوراً بدون إنشاء توثيق
- ✅ عند الانتهاء: اذكر ملخص سريع (2-3 أسطر فقط) في الرد
- ✅ إذا طلب المستخدم توثيق أو تقرير، أنشئه فقط
- ✅ احذف الملفات المؤقتة بعد الانتهاء

**مثال صحيح:**
```
المستخدم: أضف حقل email للجدول users
الرد: ✅ تم إضافة حقل email إلى جدول users
```

**مثال خاطئ:**
```
المستخدم: أضف حقل email للجدول users
الرد: تم إضافة حقل email بنجاح
[ثم ينشئ: MIGRATION_REPORT.md, CHANGES_SUMMARY.md, UPDATE_LOG.txt]
```


