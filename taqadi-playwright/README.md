# 🚗 أتمتة تقاضي - Playwright

أتمتة رفع الدعاوى على موقع تقاضي من نظام العراف باستخدام Playwright.

## ✨ المميزات

- ✅ يعمل على متصفح Chrome الفعلي (لا يُكتشف كـ bot)
- ✅ يحفظ بيانات تسجيل الدخول (تسجيل مرة واحدة فقط)
- ✅ يجلب البيانات تلقائياً من قاعدة بيانات العراف
- ✅ يملأ النموذج بضغطة واحدة
- ✅ مجاني 100%

## 📋 المتطلبات

- [Node.js](https://nodejs.org/) (الإصدار 16 أو أحدث)
- متصفح Chrome مثبت على الجهاز

## 📥 التثبيت

```bash
# انتقل لمجلد المشروع
cd taqadi-playwright

# تثبيت التبعيات
npm install

# تثبيت متصفحات Playwright
npx playwright install chromium
```

## 🚀 طريقة الاستخدام

### الخطوة 1: تحديد العقد

افتح ملف `.env` وحدد رقم العقد:

```env
CONTRACT_ID=c1d24b06-dd18-4f6a-8126-c83efaeddfb7
```

أو انسخ من `.env.example`:
```bash
copy .env.example .env
```

### الخطوة 2: جلب بيانات الدعوى

```bash
npm run fetch
```

سيتم:
- جلب بيانات العقد والعميل والسيارة
- حساب المبالغ المستحقة
- جلب المخالفات المرورية
- حفظ كل شيء في `lawsuit-data.json`

### الخطوة 3: تسجيل الدخول (مرة واحدة)

```bash
npm run login
```

سيتم:
- فتح متصفح Chrome
- الانتقال لموقع تقاضي
- انتظارك لتسجيل الدخول عبر توثيق
- حفظ بيانات الدخول في `auth.json`

### الخطوة 4: تشغيل الأتمتة

```bash
npm start
```

سيتم:
- فتح المتصفح مع بيانات الدخول المحفوظة
- الانتقال لصفحة إنشاء الدعوى
- ملء جميع الحقول تلقائياً
- انتظارك للمراجعة والاعتماد

## 📁 هيكل الملفات

```
taqadi-playwright/
├── package.json      # تبعيات المشروع
├── .env              # إعدادات (معرف العقد)
├── fetch-data.js     # جلب البيانات من العراف
├── login.js          # تسجيل الدخول وحفظ الـ cookies
├── automate.js       # السكريبت الرئيسي للأتمتة
├── lawsuit-data.json # بيانات الدعوى (يُنشأ تلقائياً)
├── auth.json         # بيانات الدخول (يُنشأ تلقائياً)
└── README.md         # هذا الملف
```

## 🔧 الأوامر المتاحة

| الأمر | الوصف |
|-------|-------|
| `npm run fetch` | جلب بيانات الدعوى من العراف |
| `npm run login` | تسجيل الدخول وحفظ الـ cookies |
| `npm start` | تشغيل الأتمتة |

## ❓ حل المشاكل

### "Cannot find module 'playwright'"
```bash
npm install
```

### "browserType.launch: Executable doesn't exist"
```bash
npx playwright install chromium
```

### "lawsuit-data.json غير موجود"
```bash
npm run fetch
```

### "فشل تسجيل الدخول"
- احذف ملف `auth.json`
- شغّل `npm run login` من جديد

## 🔒 الأمان

- ❌ لا يتم إرسال بياناتك لأي خوادم خارجية
- ✅ كل شيء يعمل محلياً على جهازك
- ✅ بيانات الدخول تُحفظ مشفرة في `auth.json`
- ⚠️ لا تشارك ملف `auth.json` مع أي شخص

## 📞 الدعم

للمساعدة، تواصل مع الدعم الفني لشركة العراف.

---

© 2024 شركة العراف لتأجير السيارات

