# 🚗 أتمتة تقاضي - Playwright

أتمتة رفع الدعاوى على موقع تقاضي من نظام العراف باستخدام Playwright.

## ✨ المميزات

- ✅ يعمل على متصفح Chrome الفعلي (لا يُكتشف كـ bot)
- ✅ يحفظ بيانات تسجيل الدخول (تسجيل مرة واحدة فقط)
- ✅ يجلب البيانات تلقائياً من قاعدة بيانات العراف
- ✅ يملأ النموذج بضغطة واحدة
- ✅ **يحمّل ويرفع المستندات تلقائياً** 📎
- ✅ مجاني 100%

## 📋 المتطلبات

- [Node.js](https://nodejs.org/) (الإصدار 16 أو أحدث)
- متصفح Chrome مثبت على الجهاز

## 📥 التثبيت

```bash
# انتقل لمجلد المشروع
cd taqadi-playwright

# تثبيت التبعيات
npm install

# تثبيت متصفحات Playwright
npx playwright install chromium
```

## 🚀 طريقة الاستخدام

### الطريقة السريعة (الكل في خطوة واحدة) ⚡

```bash
npm run all
```

يقوم بـ: جلب البيانات ← تحميل المستندات ← تشغيل الأتمتة

---

### الخطوات المنفصلة

#### الخطوة 1: جلب بيانات الدعوى

```bash
npm run fetch
```

سيطلب منك اختيار:
1. إدخال البيانات يدوياً
2. استخدام بيانات تجريبية (للاختبار)

#### الخطوة 2: تحميل المستندات 📥

```bash
npm run download
```

يحمّل المستندات من Supabase Storage:
- السجل التجاري
- شهادة IBAN
- البطاقة الشخصية
- الوكالة/التوقيع المخول

ويولّد تلقائياً:
- المذكرة الشارحة (HTML)
- كشف المستندات (HTML)
- كشف المطالبات (HTML)

#### الخطوة 3: تسجيل الدخول (مرة واحدة)

```bash
npm run login
```

سيتم:
- فتح متصفح Chrome
- الانتقال لموقع تقاضي
- انتظارك لتسجيل الدخول عبر توثيق
- حفظ بيانات الدخول في `auth.json`

#### الخطوة 4: تشغيل الأتمتة

```bash
npm start
```

سيتم:
- فتح المتصفح مع بيانات الدخول المحفوظة
- الانتقال لصفحة إنشاء الدعوى
- ملء جميع الحقول تلقائياً
- **رفع المستندات تلقائياً** 📎
- انتظارك للمراجعة والاعتماد

## 📊 تسلسل العمل

```
┌────────────────┐
│   npm run all  │ ← الكل في خطوة واحدة
└───────┬────────┘
        │
        ▼
┌────────────────┐
│  fetch-data.js │ ← جلب بيانات الدعوى
└───────┬────────┘
        │
        ▼
┌────────────────┐
│download-docs.js│ ← تحميل المستندات
└───────┬────────┘
        │
        ▼
┌────────────────┐
│  automate.js   │ ← ملء النموذج + رفع الملفات
└───────┬────────┘
        │
        ▼
┌────────────────┐
│ 📋 المراجعة   │ ← مراجعة وإعتماد الدعوى
└────────────────┘
```

## 📎 المستندات المرفوعة

| المستند | المصدر | الحالة |
|---------|--------|--------|
| المذكرة الشارحة | توليد تلقائي | ✅ |
| كشف المستندات | توليد تلقائي | ✅ |
| كشف المطالبات | توليد تلقائي | ✅ |
| السجل التجاري | Supabase | ✅ |
| شهادة IBAN | Supabase | ✅ |
| البطاقة الشخصية | Supabase | ✅ |
| التوقيع المخول | Supabase | ✅ |
| عقد الإيجار | رفع يدوي | ⏳ |

## 📁 هيكل الملفات

```
taqadi-playwright/
├── package.json        # تبعيات المشروع
├── fetch-data.js       # جلب بيانات الدعوى
├── download-docs.js    # تحميل المستندات 📥
├── login.js            # تسجيل الدخول
├── automate.js         # الأتمتة الرئيسية
├── temp/               # المستندات المحملة 📁
│   ├── السجل_التجاري.pdf
│   ├── شهادة_IBAN.pdf
│   ├── المذكرة_الشارحة.html
│   └── ...
├── lawsuit-data.json   # بيانات الدعوى
├── auth.json           # بيانات الدخول
└── README.md           # هذا الملف
```

## 🔧 الأوامر المتاحة

| الأمر | الوصف |
|-------|-------|
| `npm run all` | ⚡ الكل في خطوة واحدة |
| `npm run fetch` | جلب بيانات الدعوى |
| `npm run download` | تحميل المستندات 📥 |
| `npm run login` | تسجيل الدخول |
| `npm start` | تشغيل الأتمتة |

## ❓ حل المشاكل

### "Cannot find module 'playwright'"
```bash
npm install
```

### "browserType.launch: Executable doesn't exist"
```bash
npx playwright install chromium
```

### "lawsuit-data.json غير موجود"
```bash
npm run fetch
```

### "فشل تحميل المستندات"
- تأكد من وجود المستندات في Supabase Storage
- تأكد من الاتصال بالإنترنت

### "فشل رفع المستندات"
- قد يكون تصميم صفحة تقاضي مختلف
- ستظهر قائمة الملفات لرفعها يدوياً

### "فشل تسجيل الدخول"
- احذف ملف `auth.json`
- شغّل `npm run login` من جديد

## 🔒 الأمان

- ❌ لا يتم إرسال بياناتك لأي خوادم خارجية
- ✅ كل شيء يعمل محلياً على جهازك
- ✅ بيانات الدخول تُحفظ في `auth.json`
- ⚠️ لا تشارك ملف `auth.json` مع أي شخص

## 📞 الدعم

للمساعدة، تواصل مع الدعم الفني لشركة العراف.

---

© 2024 شركة العراف لتأجير السيارات
