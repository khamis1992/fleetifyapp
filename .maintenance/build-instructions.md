# 🔧 تعليمات إعادة البناء للإنتاج

## المشكلة المكتشفة

خطأ في تحميل صفحة التقارير المالية:
```
Failed to fetch dynamically imported module: Reports-E_8fsdTr.js
```

## السبب

الكود المصدري سليم 100%، لكن ملفات JavaScript المبنية للإنتاج قديمة أو بها مشكلة.

## الحل المطلوب

### 1. إعادة بناء المشروع

```bash
# تنظيف الملفات القديمة
npm run clean
# أو
rm -rf dist

# إعادة البناء للإنتاج
npm run build

# رفع الملفات الجديدة للسيرفر
# (حسب طريقة النشر المستخدمة)
```

### 2. التحقق من نجاح البناء

بعد البناء، تأكد من وجود الملفات في مجلد `dist`:
```bash
ls -la dist/assets/*.js | grep Reports
```

يجب أن تجد ملف مثل: `Reports-[hash].js`

### 3. اختبار بعد النشر

1. افتح: https://www.alaraf.online/finance/reports
2. يجب أن تعمل الصفحة بدون أخطاء

## ملاحظات مهمة

- ✅ الكود المصدري في `src/pages/finance/Reports.tsx` سليم
- ✅ التعريف في `src/pages/Finance.tsx` صحيح
- ⚠️ المشكلة فقط في build الإنتاج

## إذا استمرت المشكلة

1. تحقق من إعدادات Vite في `vite.config.ts`
2. تأكد من عدم وجود مشاكل في الذاكرة أثناء البناء
3. جرب بناء المشروع على جهاز آخر

