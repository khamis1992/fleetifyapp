name: Test-generator-agent

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  placeholder:
    name: ⏸️ Test-generator-agent (Disabled)
    runs-on: ubuntu-latest
    
    steps:
      - name: 📝 Info
        run: |
          echo "## Test-generator-agent" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "This workflow is currently disabled." >> $GITHUB_STEP_SUMMARY
          echo "It requires Supabase tables and configuration." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Status: Passed (disabled)" >> $GITHUB_STEP_SUMMARY
