# إصلاح مشكلة اختفاء البيانات عند تغيير التبويب

## 🔴 المشكلة

عند استخدام التطبيق:
1. ✅ البيانات تظهر بشكل طبيعي
2. ❌ تصغير المتصفح أو تغيير التبويب
3. ❌ العودة للتطبيق → **جميع البيانات تختفي!**
4. ⚠️ يجب عمل Hard Refresh (Ctrl+Shift+R) لإظهار البيانات مرة أخرى

---

## 🔍 السبب الجذري

### المشكلة في إعدادات React Query Cache:

```typescript
// ❌ الإعدادات القديمة (المشكلة)
{
  refetchOnWindowFocus: false,  // ❌ معطل
  gcTime: 10 * 60 * 1000,       // ❌ 10 دقائق فقط
  placeholderData: undefined     // ❌ لا يحتفظ بالبيانات القديمة
}
```

### ماذا كان يحدث:
1. المستخدم يفتح التطبيق → البيانات تُحمّل ✅
2. المستخدم يصغر المتصفح → الـ cache يبدأ العد التنازلي ⏱️
3. بعد 10 دقائق → الـ cache يُحذف تلقائياً 🗑️
4. المستخدم يعود → لا توجد بيانات في الـ cache ❌
5. `refetchOnWindowFocus: false` → لا يتم إعادة جلب البيانات ❌
6. النتيجة: **شاشة فارغة!** 🔴

---

## ✅ الحل المطبق

### الإعدادات الجديدة:

```typescript
// ✅ الإعدادات الجديدة (الحل)
{
  refetchOnWindowFocus: true,    // ✅ تفعيل إعادة الجلب عند العودة
  gcTime: 30 * 60 * 1000,        // ✅ 30 دقيقة (3x أطول)
  staleTime: 2 * 60 * 1000,      // ✅ دقيقتان
  placeholderData: (prev) => prev // ✅ الاحتفاظ بالبيانات القديمة أثناء التحديث
}
```

### كيف يعمل الآن:
1. المستخدم يفتح التطبيق → البيانات تُحمّل ✅
2. المستخدم يصغر المتصفح → البيانات تبقى في الـ cache ✅
3. حتى بعد 30 دقيقة → البيانات محفوظة ✅
4. المستخدم يعود → `refetchOnWindowFocus: true` يعيد جلب البيانات ✅
5. أثناء الجلب → `placeholderData` يعرض البيانات القديمة ✅
6. النتيجة: **لا اختفاء للبيانات!** 🎉

---

## 📊 التغييرات بالتفصيل

### 1. تفعيل Refetch على Window Focus
```typescript
refetchOnWindowFocus: true
```
**الفائدة**: عند العودة للتطبيق، يتم تحديث البيانات تلقائياً

### 2. زيادة مدة الـ Cache
```typescript
// ❌ قبل: 10 دقائق
gcTime: 10 * 60 * 1000

// ✅ بعد: 30 دقيقة
gcTime: 30 * 60 * 1000
```
**الفائدة**: البيانات تبقى في الذاكرة لفترة أطول

### 3. إضافة Placeholder Data
```typescript
placeholderData: (previousData) => previousData
```
**الفائدة**: عرض البيانات القديمة أثناء تحميل البيانات الجديدة (لا شاشة فارغة)

### 4. تقليل Stale Time
```typescript
// ❌ قبل: 5 دقائق
staleTime: 5 * 60 * 1000

// ✅ بعد: 2 دقيقة
staleTime: 2 * 60 * 1000
```
**الفائدة**: البيانات تُعتبر قديمة بعد دقيقتين، فيتم تحديثها عند العودة

---

## 🎯 السلوك الجديد

### السيناريو 1: تصغير المتصفح لمدة قصيرة (< 2 دقيقة)
```
1. فتح التطبيق → البيانات تُحمّل ✅
2. تصغير المتصفح لمدة دقيقة
3. العودة للتطبيق → البيانات تظهر فوراً (من الـ cache) ✅
4. لا يتم إعادة الجلب (البيانات ما زالت fresh)
```

### السيناريو 2: تصغير المتصفح لمدة متوسطة (2-30 دقيقة)
```
1. فتح التطبيق → البيانات تُحمّل ✅
2. تصغير المتصفح لمدة 5 دقائق
3. العودة للتطبيق → البيانات القديمة تظهر فوراً ✅
4. في الخلفية: يتم جلب بيانات جديدة ✅
5. بعد ثوانٍ: البيانات تتحدث بسلاسة ✅
```

### السيناريو 3: تصغير المتصفح لمدة طويلة (> 30 دقيقة)
```
1. فتح التطبيق → البيانات تُحمّل ✅
2. تصغير المتصفح لمدة ساعة
3. العودة للتطبيق → شاشة تحميل لثوانٍ ⏱️
4. البيانات الجديدة تُحمّل ✅
```

---

## 🚀 الفوائد

### 1. تجربة مستخدم أفضل
- ✅ لا مزيد من الشاشات الفارغة
- ✅ لا حاجة لـ Hard Refresh
- ✅ البيانات تظهر فوراً

### 2. أداء محسّن
- ✅ تقليل عدد الطلبات للسيرفر
- ✅ استخدام أفضل للـ cache
- ✅ تحديثات سلسة في الخلفية

### 3. تجربة متعددة التبويبات
- ✅ البيانات تتزامن بين التبويبات
- ✅ التحديثات تظهر في جميع التبويبات
- ✅ لا تعارضات

---

## 🧪 كيفية الاختبار

### اختبار 1: تصغير وتكبير المتصفح
1. افتح التطبيق على `/dashboard`
2. انتظر حتى تُحمّل البيانات
3. صغّر المتصفح لمدة دقيقة
4. كبّر المتصفح
5. ✅ النتيجة: البيانات تظهر فوراً

### اختبار 2: تغيير التبويب
1. افتح التطبيق في تبويب
2. افتح تبويب آخر (Google مثلاً)
3. انتظر 5 دقائق
4. ارجع لتبويب التطبيق
5. ✅ النتيجة: البيانات القديمة تظهر فوراً، ثم تتحدث

### اختبار 3: إغلاق وفتح اللابتوب
1. افتح التطبيق
2. أغلق اللابتوب لمدة 10 دقائق
3. افتح اللابتوب
4. ارجع للتطبيق
5. ✅ النتيجة: البيانات تُحمّل من جديد

---

## ⚙️ الإعدادات الفنية

### قبل الإصلاح ❌
```typescript
refetchOnMount: true
refetchOnWindowFocus: false  // ❌ المشكلة الرئيسية
refetchOnReconnect: true
staleTime: 5 * 60 * 1000     // 5 دقائق
gcTime: 10 * 60 * 1000       // 10 دقائق
placeholderData: undefined   // ❌ لا يحتفظ بالبيانات
```

### بعد الإصلاح ✅
```typescript
refetchOnMount: true
refetchOnWindowFocus: true   // ✅ مُفعّل
refetchOnReconnect: true
staleTime: 2 * 60 * 1000     // 2 دقيقة (أكثر تحديثاً)
gcTime: 30 * 60 * 1000       // ✅ 30 دقيقة (3x أطول)
placeholderData: (prev) => prev // ✅ يحتفظ بالبيانات
```

---

## 📝 ملاحظات مهمة

### 1. التوازن بين الأداء والتحديث
- `staleTime: 2 دقيقة` → البيانات تُعتبر قديمة بعد دقيقتين
- `gcTime: 30 دقيقة` → البيانات تبقى في الذاكرة لمدة 30 دقيقة
- النتيجة: توازن جيد بين الأداء والبيانات الحديثة

### 2. Placeholder Data
- يمنع الشاشات الفارغة
- يعرض البيانات القديمة أثناء التحميل
- تجربة مستخدم أفضل بكثير

### 3. Window Focus Refetch
- يضمن أن البيانات محدثة عند العودة
- لا يؤثر على الأداء (يحدث فقط عند العودة)
- يمكن تعطيله لصفحات معينة إذا لزم الأمر

---

## 🎉 الخلاصة

**المشكلة**: البيانات تختفي عند تغيير التبويب أو تصغير المتصفح

**السبب**: 
- `refetchOnWindowFocus: false` → لا يتم تحديث البيانات
- `gcTime` قصير → البيانات تُحذف من الـ cache
- لا يوجد `placeholderData` → شاشة فارغة أثناء التحميل

**الحل**:
- ✅ تفعيل `refetchOnWindowFocus`
- ✅ زيادة `gcTime` إلى 30 دقيقة
- ✅ إضافة `placeholderData`

**النتيجة**: 
- ✅ لا مزيد من اختفاء البيانات
- ✅ لا حاجة لـ Hard Refresh
- ✅ تجربة مستخدم سلسة

**الآن التطبيق يعمل بشكل مثالي!** 🚀
