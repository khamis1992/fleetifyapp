# Task: إضافة زر تسجيل دفعة سريعة في صفحة متابعة الإيجارات

## Objective
إضافة زر "تسجيل دفعة" في صفحة متابعة الإيجارات الشهرية بحيث يفتح نموذج الدفع السريع مع اختيار العميل تلقائياً بدلاً من البحث عنه.

## Acceptance Criteria
- [x] حذف عمود "كود العميل" من الجدول
- [x] إضافة عمود "الإجراءات" يحتوي على زر "تسجيل دفعة"
- [x] عند الضغط على الزر يفتح Dialog يحتوي نموذج الدفع السريع
- [x] يكون العميل محدد تلقائياً في النموذج (لا حاجة للبحث)
- [x] يعمل النموذج بنفس طريقة `/finance/payments/quick`

## Scope & Impact Radius
**Files modified:**
- `src/components/finance/MonthlyRentTracker.tsx` - الجدول الرئيسي
- `src/components/finance/QuickPaymentDialog.tsx` - مكون جديد للدفع السريع

**Out-of-scope:**
- تغيير منطق صفحة الدفع السريع الأصلية
- تعديل قاعدة البيانات

## Steps
- [x] Pre-flight: قراءة الملفات الحالية
- [x] إنشاء مكون `QuickPaymentDialog` للدفع السريع
- [x] تعديل `MonthlyRentTracker.tsx`:
  - [x] حذف عمود "كود العميل"
  - [x] إضافة عمود "الإجراءات"
  - [x] إضافة زر "تسجيل دفعة"
  - [x] إضافة Dialog state
- [x] اختبار الوظيفة
- [x] Build والتأكد من عدم وجود أخطاء

## Review (after merge)

### Summary of changes:
1. **مكون جديد `QuickPaymentDialog.tsx`:**
   - نافذة منبثقة لتسجيل الدفعات السريعة
   - يستقبل بيانات العميل مباشرة (لا حاجة للبحث)
   - يعرض فواتير العميل غير المدفوعة
   - يسمح باختيار فواتير متعددة
   - يدعم طرق دفع مختلفة (نقدي، تحويل بنكي، شيك)
   - يمكن إرسال الإيصال عبر واتساب

2. **تعديلات `MonthlyRentTracker.tsx`:**
   - حذف عمود "كود العميل" لتقليل عرض الجدول
   - إضافة عمود "الإجراءات" مع زر "تسجيل دفعة"
   - الزر يظهر فقط للعملاء الذين لم يدفعوا بالكامل
   - للعملاء الذين دفعوا يظهر شارة "مدفوع"

### Known limitations:
- يحتاج المستخدم للضغط على زر في كل صف بدلاً من عمل bulk payments
- الإيصال يتم تحميله كـ HTML وليس PDF

### Follow-ups:
- يمكن إضافة خيار لتسجيل دفعات متعددة لعدة عملاء دفعة واحدة
- يمكن إضافة خيار طباعة مباشرة للإيصال
