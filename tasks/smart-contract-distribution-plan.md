# نظام التوزيع الذكي للعقود - Smart Contract Distribution

**التاريخ:** 28 يناير 2026  
**الحالة:** مرحلة التخطيط

---

## 🎯 الهدف

إنشاء نظام توزيع ذكي وعادل للعقود على الموظفين بحيث:
- ✅ توزيع متوازن للعبء
- ✅ توزيع عادل للعملاء (جيدين وسيئين)
- ✅ مراعاة أداء الموظف
- ✅ مراعاة خبرة الموظف
- ✅ إمكانية التحكم اليدوي

---

## 📊 تحليل المشكلة

### السيناريوهات الحالية:
1. **توزيع عشوائي** → غير عادل
2. **توزيع يدوي** → يستغرق وقت
3. **الموظف الأول يأخذ الكل** → غير متوازن
4. **موظف واحد يأخذ كل العملاء السيئين** → غير عادل

### المشاكل:
- ❌ عدم توازن العبء
- ❌ عدم عدالة التوزيع
- ❌ إرهاق بعض الموظفين
- ❌ تأثير على الأداء

---

## 💡 الأفكار والمقترحات

### الفكرة 1: التوزيع بالتناوب (Round Robin)
**الوصف:** توزيع العقود بالتناوب على الموظفين

**المزايا:**
- ✅ بسيط وسهل التنفيذ
- ✅ توزيع متساوي للعدد

**العيوب:**
- ❌ لا يراعي صعوبة العميل
- ❌ لا يراعي أداء الموظف
- ❌ قد يحصل موظف على عملاء صعبين متتاليين

**التقييم:** ⭐⭐⭐ (3/5)

---

### الفكرة 2: التوزيع حسب العبء (Load Balancing)
**الوصف:** توزيع العقود على الموظف الأقل عبئاً

**المزايا:**
- ✅ توازن العبء
- ✅ لا أحد مُحمّل أكثر من اللازم

**العيوب:**
- ❌ لا يراعي نوعية العملاء
- ❌ قد يحصل الموظف الجديد على عملاء صعبين

**التقييم:** ⭐⭐⭐⭐ (4/5)

---

### الفكرة 3: التوزيع الذكي المتوازن (Smart Balanced Distribution) ⭐ مُوصى به
**الوصف:** توزيع ذكي يراعي عدة عوامل

**العوامل:**
1. **عدد العقود الحالية** (40%)
2. **صعوبة العملاء** (30%)
3. **أداء الموظف** (20%)
4. **خبرة الموظف** (10%)

**المزايا:**
- ✅ توزيع عادل ومتوازن
- ✅ يراعي جميع العوامل
- ✅ يحفز الأداء الجيد
- ✅ يحمي الموظفين الجدد

**العيوب:**
- ⚠️ أكثر تعقيداً
- ⚠️ يحتاج حسابات

**التقييم:** ⭐⭐⭐⭐⭐ (5/5)

---

### الفكرة 4: التوزيع حسب التخصص (Specialization)
**الوصف:** تعيين موظفين متخصصين لأنواع معينة من العملاء

**المزايا:**
- ✅ كفاءة أعلى
- ✅ خبرة متخصصة

**العيوب:**
- ❌ يحتاج تصنيف العملاء
- ❌ قد لا يكون عملي للشركات الصغيرة

**التقييم:** ⭐⭐⭐ (3/5) - للمستقبل

---

## 🎯 الحل المقترح: التوزيع الذكي المتوازن

### المعادلة:
```
نقاط الموظف = (
  (100 - عدد_العقود_الحالية) × 40% +
  (100 - متوسط_صعوبة_عقوده) × 30% +
  أداء_الموظف × 20% +
  خبرة_الموظف × 10%
)

الموظف المختار = الموظف بأعلى نقاط
```

---

## 📐 تصنيف صعوبة العملاء

### المعايير:
1. **سجل الدفع** (40%)
   - عدد الدفعات المتأخرة
   - متوسط التأخير
   - نسبة الالتزام

2. **التواصل** (30%)
   - سهولة الوصول
   - التجاوب
   - عدد الشكاوى

3. **المبلغ** (20%)
   - قيمة العقد
   - عدد الدفعات

4. **التاريخ** (10%)
   - عميل جديد أم قديم
   - سجل المشاكل

### التصنيف:
```
🟢 سهل (0-30): عميل ممتاز، يدفع بانتظام
🟡 متوسط (31-60): عميل عادي، بعض التأخيرات
🟠 صعب (61-80): عميل متأخر، يحتاج متابعة
🔴 صعب جداً (81-100): عميل مشكل، متأخر جداً
```

---

## 🔧 التنفيذ المقترح

### المرحلة 1: تصنيف العملاء
**المكونات:**
- [ ] Function: `calculate_customer_difficulty_score(customer_id)`
- [ ] View: `customer_difficulty_view`
- [ ] UI: عرض التصنيف في بطاقة العميل

**الحقول المطلوبة:**
```sql
-- في جدول customers أو view
difficulty_score INT,  -- 0-100
difficulty_level TEXT, -- easy, medium, hard, very_hard
payment_reliability DECIMAL, -- 0-100
communication_score DECIMAL, -- 0-100
```

---

### المرحلة 2: حساب قدرة الموظف
**المكونات:**
- [ ] Function: `calculate_employee_capacity(employee_id)`
- [ ] View: `employee_capacity_view`

**الحقول:**
```sql
current_contracts_count INT,
current_workload_score DECIMAL, -- 0-100
avg_customer_difficulty DECIMAL, -- 0-100
performance_score DECIMAL, -- 0-100
experience_months INT,
capacity_score DECIMAL -- 0-100 (كلما أعلى، أقدر على استقبال عقود)
```

---

### المرحلة 3: خوارزمية التوزيع
**المكونات:**
- [ ] Function: `smart_assign_contract(contract_id, options)`
- [ ] Function: `smart_bulk_assign(contract_ids[], options)`

**الخوارزمية:**
```sql
1. احسب صعوبة العقد (من العميل)
2. احصل على قائمة الموظفين المتاحين
3. احسب capacity_score لكل موظف
4. إذا كان العقد صعب:
   - اختر موظف ذو أداء عالي
   - تجنب الموظفين الجدد
5. إذا كان العقد سهل:
   - وزّع بالتساوي
   - يمكن للموظفين الجدد
6. تحقق من التوازن النهائي
7. عيّن العقد
```

---

### المرحلة 4: واجهة المستخدم
**المكونات:**
- [ ] `SmartDistributionDialog.tsx` - حوار التوزيع الذكي
- [ ] `DistributionPreview.tsx` - معاينة التوزيع
- [ ] `DistributionSettings.tsx` - إعدادات التوزيع

**الميزات:**
- [ ] اختيار الموظفين المشاركين
- [ ] اختيار العقود للتوزيع
- [ ] معاينة التوزيع قبل التطبيق
- [ ] إعدادات الأوزان (40%, 30%, 20%, 10%)
- [ ] استثناءات يدوية

---

## 🎨 تصميم الواجهة المقترح

### حوار التوزيع الذكي:
```
┌─────────────────────────────────────────────┐
│ 🤖 توزيع ذكي للعقود                        │
├─────────────────────────────────────────────┤
│ الخطوة 1: اختر الموظفين                    │
│ ☑ أحمد محمد (15 عقود) [أداء: 87%]         │
│ ☑ فاطمة علي (12 عقود) [أداء: 92%]         │
│ ☐ محمد خالد (جديد) [أداء: N/A]            │
├─────────────────────────────────────────────┤
│ الخطوة 2: اختر العقود                      │
│ ☑ 25 عقد جديد                              │
│ ☑ 10 عقود غير معيّنة                      │
├─────────────────────────────────────────────┤
│ الخطوة 3: معاينة التوزيع                   │
│ أحمد محمد: +8 عقود (3 سهل، 5 متوسط)      │
│ فاطمة علي: +7 عقود (4 سهل، 3 متوسط)      │
├─────────────────────────────────────────────┤
│ [إلغاء] [معاينة] [تطبيق التوزيع]          │
└─────────────────────────────────────────────┘
```

---

## 📊 معايير التوزيع المقترحة

### 1. عدد العقود (40%)
```
الموظف بأقل عقود يحصل على نقاط أعلى
نقاط = (max_contracts - current_contracts) / max_contracts × 100
```

### 2. صعوبة العملاء (30%)
```
الموظف بأقل متوسط صعوبة يحصل على العملاء الصعبين
نقاط = (100 - avg_difficulty) × عامل_التوازن
```

### 3. أداء الموظف (20%)
```
الموظف بأداء أعلى يحصل على عملاء أصعب
نقاط = performance_score
```

### 4. خبرة الموظف (10%)
```
الموظف الأكثر خبرة يحصل على عملاء أصعب
نقاط = MIN(experience_months / 12 × 100, 100)
```

---

## 🔄 سيناريوهات الاستخدام

### السيناريو 1: توزيع 20 عقد جديد
```
المدخلات:
- 20 عقد جديد
- 5 موظفين متاحين

الخوارزمية:
1. صنّف العقود حسب الصعوبة
   - 8 سهل
   - 7 متوسط
   - 5 صعب

2. رتّب الموظفين حسب القدرة
   - أحمد (87%) - 15 عقود
   - فاطمة (92%) - 12 عقود
   - محمد (75%) - 18 عقود
   - سارة (جديدة) - 5 عقود

3. وزّع:
   - فاطمة: 2 صعب، 2 متوسط، 1 سهل (5 عقود)
   - أحمد: 2 صعب، 2 متوسط، 1 سهل (5 عقود)
   - محمد: 1 صعب، 2 متوسط، 1 سهل (4 عقود)
   - سارة: 0 صعب، 1 متوسط، 5 سهل (6 عقود)

النتيجة: توزيع عادل ومتوازن ✅
```

---

### السيناريو 2: إعادة توازن العبء
```
الحالة الحالية:
- أحمد: 25 عقود (10 صعب)
- فاطمة: 10 عقود (2 صعب)

الإجراء:
1. احسب عدم التوازن
2. اقترح نقل 5 عقود من أحمد إلى فاطمة
3. اختر العقود الأسهل للنقل
4. نفّذ بعد الموافقة

النتيجة: توازن أفضل ✅
```

---

## 🗄️ Database Schema المطلوب

### 1. جدول customer_difficulty_scores
```sql
CREATE TABLE customer_difficulty_scores (
  id UUID PRIMARY KEY,
  customer_id UUID REFERENCES customers(id),
  
  -- Scores (0-100)
  payment_score DECIMAL, -- كلما أقل، أسوأ
  communication_score DECIMAL,
  amount_score DECIMAL,
  history_score DECIMAL,
  
  -- Overall
  difficulty_score DECIMAL, -- 0-100 (كلما أعلى، أصعب)
  difficulty_level TEXT, -- easy, medium, hard, very_hard
  
  -- Metadata
  last_calculated_at TIMESTAMPTZ,
  calculation_details JSONB
);
```

### 2. جدول employee_capacity
```sql
CREATE TABLE employee_capacity (
  id UUID PRIMARY KEY,
  employee_id UUID REFERENCES profiles(id),
  
  -- Current state
  current_contracts INT,
  current_workload_score DECIMAL,
  avg_customer_difficulty DECIMAL,
  
  -- Capacity
  max_recommended_contracts INT,
  capacity_score DECIMAL, -- 0-100
  is_available BOOLEAN,
  
  -- Metadata
  last_updated_at TIMESTAMPTZ
);
```

### 3. جدول distribution_rules
```sql
CREATE TABLE distribution_rules (
  id UUID PRIMARY KEY,
  company_id UUID,
  
  -- Rule settings
  rule_name TEXT,
  distribution_strategy TEXT, -- round_robin, load_balance, smart
  
  -- Weights
  contracts_count_weight DECIMAL DEFAULT 0.40,
  difficulty_weight DECIMAL DEFAULT 0.30,
  performance_weight DECIMAL DEFAULT 0.20,
  experience_weight DECIMAL DEFAULT 0.10,
  
  -- Constraints
  max_contracts_per_employee INT,
  min_performance_for_hard_customers DECIMAL,
  protect_new_employees BOOLEAN DEFAULT true,
  
  is_active BOOLEAN DEFAULT true
);
```

### 4. جدول distribution_history
```sql
CREATE TABLE distribution_history (
  id UUID PRIMARY KEY,
  company_id UUID,
  
  -- Distribution details
  distribution_date TIMESTAMPTZ,
  strategy_used TEXT,
  contracts_distributed INT,
  employees_involved INT,
  
  -- Results
  distribution_details JSONB,
  fairness_score DECIMAL,
  
  performed_by UUID
);
```

---

## 🔧 Functions المطلوبة

### 1. حساب صعوبة العميل
```sql
CREATE FUNCTION calculate_customer_difficulty(p_customer_id UUID)
RETURNS DECIMAL AS $$
DECLARE
  v_payment_score DECIMAL;
  v_communication_score DECIMAL;
  v_difficulty DECIMAL;
BEGIN
  -- حساب نقاط الدفع (كم دفعة متأخرة؟)
  SELECT 
    CASE 
      WHEN COUNT(*) = 0 THEN 100
      ELSE 100 - (COUNT(*) FILTER (WHERE payment_date > due_date) / COUNT(*) * 100)
    END INTO v_payment_score
  FROM payments
  WHERE customer_id = p_customer_id;
  
  -- حساب نقاط التواصل
  -- ... المزيد من الحسابات
  
  -- الصعوبة النهائية
  v_difficulty := (
    (100 - v_payment_score) * 0.40 +
    (100 - v_communication_score) * 0.30 +
    -- ... باقي العوامل
  );
  
  RETURN v_difficulty;
END;
$$ LANGUAGE plpgsql;
```

### 2. حساب قدرة الموظف
```sql
CREATE FUNCTION calculate_employee_capacity(p_employee_id UUID)
RETURNS DECIMAL AS $$
DECLARE
  v_contracts_count INT;
  v_avg_difficulty DECIMAL;
  v_performance DECIMAL;
  v_capacity DECIMAL;
BEGIN
  -- عدد العقود الحالية
  SELECT COUNT(*) INTO v_contracts_count
  FROM contracts
  WHERE assigned_to_profile_id = p_employee_id;
  
  -- متوسط صعوبة عقوده
  -- ... حسابات
  
  -- القدرة النهائية
  v_capacity := (
    (20 - v_contracts_count) / 20 * 100 * 0.40 +
    (100 - v_avg_difficulty) * 0.30 +
    v_performance * 0.20 +
    -- ... خبرة
  );
  
  RETURN v_capacity;
END;
$$ LANGUAGE plpgsql;
```

### 3. التوزيع الذكي
```sql
CREATE FUNCTION smart_distribute_contracts(
  p_contract_ids UUID[],
  p_employee_ids UUID[],
  p_strategy TEXT DEFAULT 'smart'
) RETURNS TABLE(
  contract_id UUID,
  employee_id UUID,
  reason TEXT
) AS $$
BEGIN
  -- الخوارزمية الذكية
  -- 1. صنّف العقود
  -- 2. رتّب الموظفين
  -- 3. وزّع بذكاء
  -- 4. تحقق من التوازن
  
  RETURN QUERY
  -- ... النتائج
END;
$$ LANGUAGE plpgsql;
```

---

## 🎨 واجهة المستخدم المقترحة

### 1. زر "توزيع ذكي" في TeamManagement
```typescript
<Button
  onClick={() => setShowSmartDistribution(true)}
  className="bg-gradient-to-r from-purple-500 to-indigo-600"
>
  <Sparkles className="ml-2 h-4 w-4" />
  توزيع ذكي
</Button>
```

### 2. حوار التوزيع (3 خطوات)
```
الخطوة 1: اختيار الموظفين
- قائمة بجميع الموظفين
- عرض عدد العقود الحالية
- عرض الأداء
- checkbox لكل موظف

الخطوة 2: اختيار العقود
- قائمة بالعقود غير المعيّنة
- عرض صعوبة كل عقد (🟢🟡🟠🔴)
- checkbox لكل عقد
- أو "تحديد الكل"

الخطوة 3: معاينة وتطبيق
- جدول بالتوزيع المقترح
- عدد العقود لكل موظف
- توزيع الصعوبات
- نقاط العدالة (Fairness Score)
- زر "تطبيق"
```

### 3. إعدادات التوزيع
```
- استراتيجية التوزيع (Round Robin / Load Balance / Smart)
- الأوزان (يمكن تعديلها)
- الحد الأقصى للعقود لكل موظف
- حماية الموظفين الجدد
- الحد الأدنى للأداء للعملاء الصعبين
```

---

## 📊 مؤشرات الأداء (KPIs)

### لقياس نجاح النظام:
1. **Fairness Score** - نقاط العدالة
   - انحراف معياري لعدد العقود
   - انحراف معياري لصعوبة العملاء

2. **Balance Score** - نقاط التوازن
   - الفرق بين أكثر وأقل موظف عبئاً

3. **Performance Impact** - تأثير على الأداء
   - هل تحسن أداء الموظفين؟
   - هل قل معدل الشكاوى؟

---

## 🎯 خطة التنفيذ

### المرحلة 1: البنية التحتية (يوم 1)
- [ ] إنشاء الجداول
- [ ] إنشاء Functions الأساسية
- [ ] إنشاء Views

### المرحلة 2: حساب الصعوبة (يوم 2)
- [ ] Function لحساب صعوبة العميل
- [ ] تطبيق على جميع العملاء
- [ ] عرض في الواجهة

### المرحلة 3: حساب القدرة (يوم 3)
- [ ] Function لحساب قدرة الموظف
- [ ] تطبيق على جميع الموظفين
- [ ] عرض في الواجهة

### المرحلة 4: خوارزمية التوزيع (يوم 4-5)
- [ ] تطوير الخوارزمية
- [ ] اختبار شامل
- [ ] تحسينات

### المرحلة 5: الواجهة (يوم 6-7)
- [ ] SmartDistributionDialog
- [ ] DistributionPreview
- [ ] DistributionSettings
- [ ] التكامل

---

## 💡 مقترحات إضافية

### 1. التعلم الآلي (مستقبلي)
- تحليل البيانات التاريخية
- تحسين التوزيع تلقائياً
- توقع نجاح التعيين

### 2. التوزيع الجغرافي
- إذا كان العملاء في مناطق مختلفة
- تعيين موظفين حسب القرب

### 3. التخصص
- موظفين متخصصين في أنواع معينة
- سيارات فاخرة، شاحنات، إلخ

### 4. الأتمتة الكاملة
- توزيع تلقائي عند إنشاء عقد جديد
- إعادة توازن تلقائية أسبوعياً

---

## ⚠️ التحديات المتوقعة

### 1. تصنيف العملاء الجدد
**المشكلة:** لا يوجد تاريخ  
**الحل:** افتراض "متوسط" حتى يتم جمع بيانات

### 2. الموظفين الجدد
**المشكلة:** لا يوجد أداء  
**الحل:** البدء بعملاء سهلين فقط

### 3. عدم التوازن الشديد
**المشكلة:** جميع العقود صعبة  
**الحل:** توزيع بالتساوي مع تحذير

### 4. رفض الموظف
**المشكلة:** موظف لا يريد عقد معين  
**الحل:** إمكانية الاستثناء اليدوي

---

## 🧪 خطة الاختبار

### 1. اختبار التصنيف
- [ ] عميل ممتاز → سهل
- [ ] عميل متأخر → صعب
- [ ] عميل جديد → متوسط

### 2. اختبار التوزيع
- [ ] 10 عقود على 3 موظفين
- [ ] التحقق من العدالة
- [ ] التحقق من التوازن

### 3. اختبار الحالات الخاصة
- [ ] جميع الموظفين مشغولين
- [ ] جميع العقود صعبة
- [ ] موظف واحد فقط

---

## 📈 مؤشرات النجاح

### بعد التطبيق:
- ✅ انحراف معياري أقل من 20% في عدد العقود
- ✅ كل موظف لديه mix من السهل والصعب
- ✅ لا موظف لديه أكثر من 70% عملاء صعبين
- ✅ الموظفون الجدد يبدأون بعملاء سهلين
- ✅ وقت التوزيع أقل من 5 ثواني

---

## 🎯 التوصية النهائية

### للبدء:
1. **ابدأ بسيط** - نظام Load Balancing أولاً
2. **أضف التصنيف** - صنّف العملاء
3. **طوّر الذكاء** - أضف العوامل تدريجياً
4. **اختبر وحسّن** - راقب النتائج وحسّن

### الأولوية:
```
أولوية عالية:
1. ✅ حساب صعوبة العميل
2. ✅ حساب قدرة الموظف
3. ✅ خوارزمية التوزيع الأساسية

أولوية متوسطة:
4. ⏳ واجهة المستخدم
5. ⏳ معاينة التوزيع
6. ⏳ إعدادات متقدمة

أولوية منخفضة:
7. ⏳ التعلم الآلي
8. ⏳ الأتمتة الكاملة
```

---

## ⏱️ التقدير الزمني

```
المرحلة 1 (البنية):        3 ساعات
المرحلة 2 (الصعوبة):       4 ساعات
المرحلة 3 (القدرة):        3 ساعات
المرحلة 4 (الخوارزمية):    6 ساعات
المرحلة 5 (الواجهة):       5 ساعات
الاختبار والتحسين:        3 ساعات
─────────────────────────────────
الإجمالي:                  ~24 ساعة (3 أيام عمل)
```

---

## 💰 القيمة المضافة

### للشركة:
- ⬆️ كفاءة أعلى في التوزيع
- ⬆️ عدالة أفضل
- ⬆️ رضا الموظفين
- ⬆️ أداء أفضل

### للموظفين:
- ⬆️ عبء متوازن
- ⬆️ عملاء متنوعين
- ⬆️ فرص عادلة
- ⬆️ تحفيز أفضل

---

## 📝 الخلاصة

**نظام التوزيع الذكي سيحسّن العدالة والكفاءة بشكل كبير!**

### الخطوات التالية:
1. مراجعة الخطة والموافقة عليها
2. البدء بالمرحلة 1 (البنية التحتية)
3. تطوير تدريجي
4. اختبار مستمر

---

**هل تريد البدء بتنفيذ هذا النظام؟**

**أوصي بالبدء بنظام بسيط (Load Balancing) ثم التطوير تدريجياً.**
