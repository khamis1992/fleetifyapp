# Employee Workspace - Phase 3: Management Dashboard
**تاريخ البدء:** 28 يناير 2026  
**الحالة:** قيد التنفيذ

## 🎯 الهدف

إنشاء لوحة تحكم للإدارة لمراقبة أداء الفريق وتوزيع العقود على الموظفين.

---

## 📋 المهام الرئيسية

### 1. صفحة إدارة الفريق (Team Management)
**المسار:** `/team-management`

**المكونات:**
- [ ] `TeamManagementPage.tsx` - الصفحة الرئيسية
- [ ] `EmployeeCard.tsx` - بطاقة الموظف
- [ ] `EmployeePerformanceChart.tsx` - رسم بياني للأداء
- [ ] `TeamStatsOverview.tsx` - نظرة عامة على الفريق

**الميزات:**
- [ ] عرض قائمة الموظفين
- [ ] إحصائيات كل موظف (عقود، أداء، مهام)
- [ ] فلترة وبحث في الموظفين
- [ ] عرض تفاصيل الموظف

---

### 2. واجهة توزيع العقود (Contract Assignment)
**المكونات:**
- [x] `ContractAssignmentDialog.tsx` - حوار تعيين عقد
- [x] `BulkAssignmentDialog.tsx` - تعيين جماعي
- [ ] `AssignmentHistory.tsx` - سجل التعيينات
- [ ] `AutoAssignmentSettings.tsx` - إعدادات التوزيع التلقائي

**الميزات:**
- [x] تعيين عقد لموظف
- [x] تعيين عدة عقود دفعة واحدة
- [x] إعادة تعيين عقد (من خلال التعيين)
- [ ] إلغاء تعيين
- [ ] توزيع تلقائي ذكي

---

### 3. جدول أداء الموظفين (Performance Table)
**المكونات:**
- [ ] `PerformanceTable.tsx` - جدول الأداء
- [ ] `PerformanceFilters.tsx` - فلاتر الأداء
- [ ] `PerformanceComparison.tsx` - مقارنة الأداء
- [ ] `PerformanceExport.tsx` - تصدير البيانات

**الميزات:**
- [ ] جدول شامل لأداء جميع الموظفين
- [ ] ترتيب حسب المعايير المختلفة
- [ ] فلترة حسب الفترة الزمنية
- [ ] مقارنة بين الموظفين
- [ ] تصدير إلى Excel/CSV

---

### 4. تقارير مفصلة (Detailed Reports)
**المكونات:**
- [ ] `ReportsPage.tsx` - صفحة التقارير
- [ ] `TeamPerformanceReport.tsx` - تقرير أداء الفريق
- [ ] `IndividualReport.tsx` - تقرير فردي
- [ ] `ContractDistributionReport.tsx` - تقرير توزيع العقود
- [ ] `ReportGenerator.tsx` - منشئ التقارير

**الميزات:**
- [ ] تقرير أداء الفريق الشهري
- [ ] تقارير فردية لكل موظف
- [ ] تقرير توزيع العقود
- [ ] رسوم بيانية تفاعلية
- [ ] تصدير PDF

---

## 🗄️ Database Requirements

### Views جديدة:
```sql
-- Team Performance Summary
CREATE VIEW team_performance_summary AS
SELECT 
  profile_id,
  COUNT(DISTINCT contract_id) as total_contracts,
  AVG(performance_score) as avg_performance,
  SUM(total_collected) as total_collected,
  -- ... المزيد
FROM employee_performance_view
GROUP BY profile_id;
```

### Functions جديدة:
```sql
-- Assign Contract to Employee
CREATE FUNCTION assign_contract_to_employee(
  p_contract_id UUID,
  p_employee_id UUID,
  p_assigned_by UUID,
  p_notes TEXT
) RETURNS void;

-- Bulk Assign Contracts
CREATE FUNCTION bulk_assign_contracts(
  p_contract_ids UUID[],
  p_employee_id UUID,
  p_assigned_by UUID
) RETURNS void;

-- Get Team Performance
CREATE FUNCTION get_team_performance(
  p_company_id UUID,
  p_start_date DATE,
  p_end_date DATE
) RETURNS TABLE(...);
```

---

## 🎨 التصميم

### الألوان:
- **Primary**: Teal (للإدارة)
- **Success**: Green (أداء جيد)
- **Warning**: Orange (يحتاج تحسين)
- **Danger**: Red (أداء ضعيف)

### Layout:
```
┌─────────────────────────────────────────┐
│ Header: Team Management                 │
├─────────────────────────────────────────┤
│ Stats Overview (4 cards)                │
├─────────────────────────────────────────┤
│ ┌─────────────┐ ┌───────────────────┐  │
│ │  Filters    │ │  Employee List    │  │
│ │  & Search   │ │  (Cards/Table)    │  │
│ │             │ │                   │  │
│ └─────────────┘ └───────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 🔐 Permissions

### Role-based Access:
- **Admin**: كل الصلاحيات
- **Manager**: عرض + تعيين عقود
- **Employee**: لا يمكنه الوصول

### RLS Policies:
```sql
-- Only admins and managers can access team data
CREATE POLICY "team_management_access" ON profiles
FOR SELECT USING (
  auth.uid() IN (
    SELECT user_id FROM profiles 
    WHERE role IN ('admin', 'manager')
  )
);
```

---

## 📊 الأولويات

### المرحلة 3.1 (الأساسيات):
1. ✅ صفحة إدارة الفريق
2. ✅ عرض قائمة الموظفين
3. ✅ واجهة تعيين العقود
4. ✅ جدول الأداء الأساسي

### المرحلة 3.2 (المتقدمة):
1. ⏳ التوزيع التلقائي
2. ⏳ التقارير المفصلة
3. ⏳ الرسوم البيانية
4. ⏳ التصدير

---

## 🧪 Testing Checklist

- [ ] عرض قائمة الموظفين
- [ ] تعيين عقد لموظف
- [ ] إعادة تعيين عقد
- [ ] عرض أداء الموظف
- [ ] فلترة وبحث
- [ ] Permissions تعمل بشكل صحيح
- [ ] Mobile responsive

---

## 📝 ملاحظات

### الاعتبارات:
- البساطة أولاً - لا نريد تعقيد
- سرعة التحميل مهمة
- Mobile-friendly
- Real-time updates حيثما أمكن

### Dependencies:
- Phase 1 ✅ (MVP)
- Phase 2 ✅ (Dialogs)
- Supabase RLS policies
- React Query for data fetching

---

**سأبدأ بالمرحلة 3.1 (الأساسيات) أولاً!**
