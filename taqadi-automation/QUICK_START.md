# دليل التشغيل السريع - أتمتة تقاضي

## 🚀 البدء السريع

### الخطوة 1: تشغيل سيرفر الأتمتة

افتح terminal جديد وشغل السيرفر:

```bash
cd taqadi-automation
node server.js
```

**مهم:** اترك هذا Terminal مفتوحاً - السيرفر يجب أن يبقى يعمل في الخلفية.

ستظهر رسالة:
```
🚀 Taqadi Automation API running on port 3001
📍 http://localhost:3001
```

### الخطوة 2: استخدام الأتمتة من FleetifyApp

1. افتح FleetifyApp: `http://localhost:8080`
2. اذهب إلى **الشؤون القانونية** → **إدارة المتعثرات**
3. اختر عقد متعثر واضغط **تجهيز دعوى**
4. في صفحة تجهيز الدعوى:
   - اضغط **توليد جميع المستندات** (انتظر حتى تصبح 5/5)
   - اضغط **رفع إلى تقاضي (أتمتة)** 🎯

### الخطوة 3: المراقبة

- سيفتح متصفح Chrome تلقائياً
- ستشاهد الأتمتة تعمل خطوة بخطوة
- إذا احتجت لإيقافها، اضغط زر **إيقاف الأتمتة**

## 📋 المتطلبات الأولية

### 1. تسجيل الدخول في تقاضي (مرة واحدة فقط)

قبل أول استخدام:
```bash
cd taqadi-automation
node src/test-setup.js
```

- سيفتح متصفح Chrome
- سجل الدخول في تقاضي يدوياً
- أغلق المتصفح
- الجلسة ستُحفظ في `taqadi-user-data/`

### 2. تسجيل الدخول في FleetifyApp

في المتصفح الذي سيفتحه Puppeteer، يجب تسجيل الدخول في FleetifyApp يدوياً في أول مرة.

## ⚙️ الإعدادات

### تغيير بيانات الأطراف

عدّل الملف `src/index.js`:

```javascript
const partiesData = {
  plaintiff1: {
    name: 'اسم الشركة',
    type: 'شركة',
    nationalId: '123456789',
    // ...
  },
  // ...
};
```

### تغيير بيانات الدعوى

عدّل الملف `src/index.js`:

```javascript
const taqadiData = {
  caseTitle: 'مطالبة مالية',
  caseType: 'مدني',
  // ...
};
```

## 🔍 استكشاف الأخطاء

### السيرفر لا يعمل

```bash
# تحقق من أن المنفذ 3001 غير مستخدم
netstat -ano | findstr :3001

# إذا كان مستخدماً، أوقف العملية أو غيّر المنفذ في server.js
```

### الأتمتة تفشل

1. **تحقق من السجلات:**
   ```bash
   cd taqadi-automation
   type logs\automation.log
   ```

2. **تحقق من الصور:**
   - افتح مجلد `logs/`
   - شاهد آخر صورة تم التقاطها

3. **الأخطاء الشائعة:**
   - ❌ "السيرفر غير متاح" → تأكد من تشغيل `node server.js`
   - ❌ "لم يتم العثور على زر" → المستندات غير جاهزة (يجب أن تكون 5/5)
   - ❌ "فشل تسجيل الدخول" → سجل الدخول في FleetifyApp يدوياً في المتصفح

## 📊 المراحل

الأتمتة تمر بـ 6 مراحل:

1. ✅ **تنزيل المستندات** من FleetifyApp
2. ✅ **نوع الدعوى** - اختيار "مدني"
3. ✅ **تفاصيل الدعوى** - ملء العنوان والوقائع
4. ✅ **أطراف الدعوى** - إضافة المدعي والمدعى عليه
5. ✅ **المستندات** - رفع جميع الملفات
6. ✅ **الاعتماد والإرسال** - إتمام الدعوى

## 🎯 نصائح

- **لا تغلق المتصفح** أثناء الأتمتة
- **لا تتدخل يدوياً** في المتصفح أثناء العمل
- **راقب Terminal** لمتابعة التقدم
- **احفظ رقم القضية** الذي يظهر في النهاية

## 🔄 التحديثات المستقبلية

- [ ] تسجيل دخول تلقائي في FleetifyApp
- [ ] استخراج البيانات ديناميكياً من FleetifyApp
- [ ] إضافة retry logic للمراحل الفاشلة
- [ ] دعم multiple contracts في نفس الوقت
