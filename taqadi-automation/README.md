# Taqadi Automation - أتمتة رفع الدعاوى

## 🚀 البدء السريع

**للاستخدام المباشر، اقرأ:** [QUICK_START.md](./QUICK_START.md)

## التثبيت

```bash
cd taqadi-automation
npm install
```

## الاستخدام

### ✅ الطريقة الموصى بها: عبر FleetifyApp

1. **شغّل السيرفر مرة واحدة:**
```bash
node server.js
```

2. **استخدم من FleetifyApp:**
   - افتح صفحة تجهيز الدعوى
   - اضغط زر **"رفع إلى تقاضي (أتمتة)"**
   - راقب التقدم في المتصفح

3. **للإيقاف:**
   - اضغط زر **"إيقاف الأتمتة"** في FleetifyApp
   - أو أغلق المتصفح

### 🧪 للاختبار المباشر

```bash
npm start [contractId]
```

مثال:
```bash
npm start f2ecdec0-2038-45d3-92ac-3f3d455627bb
```

## الهيكل

```
taqadi-automation/
├── src/
│   ├── config/
│   │   └── selectors.js          # جميع الـ Selectors
│   ├── services/
│   │   ├── browser.service.js    # إدارة المتصفح
│   │   └── file.service.js       # تنزيل وفك ضغط الملفات
│   ├── stages/
│   │   ├── stage0-case-type.js   # نوع الدعوى
│   │   ├── stage1-case-details.js # تفاصيل الدعوى
│   │   ├── stage2-parties.js     # أطراف الدعوى
│   │   ├── stage3-documents.js   # المستندات
│   │   ├── stage4-fees.js        # الرسوم
│   │   └── stage5-summary.js     # الملخص والاعتماد
│   ├── utils/
│   │   ├── wait.js               # دوال الانتظار
│   │   └── logger.js             # السجلات
│   └── index.js                  # نقطة البداية
├── downloads/                     # مجلد التنزيلات
├── work/                          # مجلد العمل (ملفات مستخرجة)
├── logs/                          # السجلات والصور
└── package.json
```

## ملاحظات مهمة

1. **تسجيل الدخول**: يجب تسجيل الدخول في تقاضي مرة واحدة يدوياً، وسيتم حفظ الجلسة في `taqadi-user-data/`.

2. **IP Restriction**: يجب تشغيل الأتمتة من قطر (IP قطري).

3. **المراقبة**: المتصفح يعمل في وضع `headless: false` للمراقبة والتطوير.

4. **الصور**: يتم التقاط صور للشاشة بعد كل مرحلة في مجلد `logs/`.

5. **السجلات**: جميع العمليات تُسجل في `logs/automation.log`.

## استكشاف الأخطاء

- إذا فشلت مرحلة معينة، راجع الصور في `logs/`
- راجع `logs/automation.log` لمعرفة تفاصيل الخطأ
- تأكد من أن صفحة تجهيز الدعوى تحتوي على جميع المستندات الجاهزة

## التطوير

لتطوير مرحلة معينة:
1. افتح الملف المقابل في `src/stages/`
2. عدّل الـ selectors في `src/config/selectors.js` إذا لزم الأمر
3. اختبر المرحلة بشكل منفصل
